# 0.9.2 开发计划

状态：本地源码收口归档；等待 Wing 发布后升级精确依赖并打包；未发布

Owner：KT Auto Code maintainers

## 基线与发布边界

- Auto 从 0.9.1 本地冻结 `2e3849fd527bfbf040ebae7116341c4af67446a6` 建立 `codex/v0.9.2`，继承已修正的滚动、Primary 联动、Block Header、Git 分支选择和安全检查。
- Wing 联调候选为固定工作树 `worktrees/phoenix-wing-working` 的 `v0.7.4@d2f73a48409c195a500787c16510c7d05aab1ea0`，tree `9d2c829d51e5f7d6c8687e8cff03aabbefc0988f`；主库 `develop` 已同步。该节点与最初归档的 `89deb20` 内容相同，仅提交信息发生整理。
- Wing 已将 `ec38785b` 起的五个节点及未提交修改压缩归档，根包 `0.7.4`、`code-core/kt-codegen@0.6.6`、生成规则 `1.0.2`；`079292d4bc` 仅摘入历史发布记录，没有合并其节点。早期 `db53540` / `d14dc9e` 回执只描述首批冻结状态。
- 0.9.0 不正式发布；旧源码与 VSIX 保留归档，不覆盖 0.9.1 冻结记录。后续修改只在开发 worktree 进行。
- 本轮授权实现、本地验证和归档；不 push、tag、npm publish 或上传 Marketplace。未发布的 Wing 候选不写入 Auto Registry 依赖或 lockfile，正式发布须另过精确 Registry 来源门禁。

## 本轮归档与发布准备（2026-09-10）

- 按用户指定，把 `0807aeaec2d` 起的三个节点（截至 `7f1dcd1`）及未提交改动压缩为一个节点，父节点保留 `2e3849fd527bfbf040ebae7116341c4af67446a6`；再 cherry-pick 到主库 `user`。主库归档前为 `1bf77b96f0e5a78756c565d617339edb37320903`，已核验其 tree 与上述父节点相同。压缩前完整工作树、暂存区和原历史保留本地备份引用。
- 归档包含下方四项、正式 YAML 探测/原生编辑/清理、当前工作目录范围与日志分离、临时 Right 清理入口、编译工具 Preview 草稿/导入/配置/脚本/任务闭环、Combo 回填注释与多主题源码修正。正式 BUILD_MANIFEST 导入和 provider 后续没有因此完成，见[后续计划](编译工具/后续原型计划.md)。归档期间不开展独立浮窗或其他新功能。
- 最终受控 `KTC_TEST_NATIVE_CMAKE=1 pnpm ext:dev:prepare` 通过：Auto **257 文件 / 1923 测试**，本地 Wing 声明类型检查、六包来源校验通过，consumer Registry Wing 输入为 **0**；CAD 独立 Registry **14 文件 / 50 测试**通过。架构 **266 source / 27 pure graphs / 25 view roots**、文档 **119 份**通过。首次全量暴露的两处旧 Primary 清理测试夹具已按 Right 归属更新后完整重跑通过。
- 真实 VS Code Extension Host 在自建临时工作区的既有代表流程再次通过；这不是新清理 Right 的完整人工验收，也不补签 Windows。清理后端真实删除只用临时夹具，没有用于用户工程。
- 新增源码/文档扫描未发现真实密钥或个人主目录路径；唯一凭据 URL 命中属于 `example.invalid` 安全拒绝测试。截图为裁剪后的表格示例。日志、旧 VSIX、源码备份和机器来源回执仅本地保存，不提交；源码点检不替代后续正式 VSIX 隐私检查。
- 用户明确：**等 Wing 发布完成再打包**。本轮不新建 VSIX、不覆盖首批冻结制品、不发布 npm、不 push/tag 或上传 Marketplace。2026-09-10 对 `code-core@0.6.6` 的只读 Registry 查询仍为 `E404`，不能用本地联调成功代替正式依赖可用。

后续打包前按 Wing 实际发布回执核验以下精确版本；表格是目标，不表示当前 lockfile 已升级：

| 包 | 当前 Registry 锁定 | Wing 联调/待消费目标 |
| --- | --- | --- |
| `@phoenix-wing/code-core` | `0.6.4` | `0.6.6` |
| `@phoenix-wing/kt-codegen` | `0.6.4` | `0.6.6` |
| `@phoenix-wing/git-core` | `0.6.4` | `0.6.4` |
| `@phoenix-wing/git-node` | `0.6.4` | `0.6.5` |
| `@phoenix-wing/run-core` | `0.6.3` | `0.6.4` |
| `@phoenix-wing/run-node` | `0.6.3` | `0.6.4` |

Wing 对应包就绪后，更新 Auto 精确 manifest、lockfile 与依赖版本映射；以 Registry 实际运行时复验生成规则 `1.0.2` 和全部门禁。依赖升级另留本地提交，不事后重写本次归档证据。正式包从确认的干净 detached release worktree 执行 `release:preflight`、`release:check`、`release:receipt`，并检查 VSIX 无本机路径与凭据；详见[发布流程](VS%20Code%20插件发布.md)。

手工点检保持三组：**1. 清理 Right 入口/四 Block/原生编辑/当前目录范围；2. 编译工具原型；3. Windows 多主题 Combo、CAA/clang-format 与脚本首行待定位**。完整步骤及已通过/待测边界见[后续点检表](编译工具/后续原型计划.md#本轮手工点检清单)。

## 四项功能范围

1. [x] **Git 合并区间 Right 原型**：按[专项计划](0.9.2-Git合并区间原型计划.md)共享正式展示层；内存模拟打开/关闭/重开、分支确认与失败、分页、主线区间选择和预检。Preview 不运行真实 checkout、合并或历史改写，不改 Git 核心算法。
2. [x] **自动代码 JSON 标签**：Panel 创建与状态更新均使用 JSON 文件名；同名文件不追加目录。保留 URI 去重、一 JSON 一 View、内部 Header、Primary 联动和文档草稿，补齐创建/重开/更新回归。
3. [x] **构造函数 END / clang-format 对齐**：用户已重新授权实施，解除此前“仅备忘”限制。结束两行按后续初始化项或单独 `{` 的缩进对齐；在 Wing 生成/替换层修正，最小夹具接入真实回归，增加换行及重入安全检查。兼容修错将规则版本从 1.0.0 升至 1.0.1，Auto 缓存及来源门禁同步，拒绝旧计划；不批量改用户源码。实际 Windows clang-format 版本、配置和运行证据须单列，不能用 macOS 测试冒充。
4. [x] **PnwCombo 迁移收尾**：先核查已迁入 Wing 的实现，保留包/tag/model/`select/remove/clear` 事件和视觉；Auto 只留必要薄适配。核对 200 项边界、弹层避让、键盘焦点、删除原因、点击外部关闭及无障碍。新增能力只按真实消费者证据提炼，不重复迁移、不顺带替换简单静态 select。见[组件记录](PnwCombo组件提炼TODO.md)。

## 首批冻结时的排除项

以下是首批内测冻结时的边界。用户随后已授权清理与编译工具后续，当前实现及三组点检见[后续原型计划](编译工具/后续原型计划.md)；不回改旧 VSIX 的范围或验证回执。

- 清理对话框快捷栏、Phoenix 缓存清理及其执行策略：不开启。
- 编译工具后续、统一项目表或新编排能力：不开启。
- 不重构外层 Primary Shell，不改已有工具的关闭/MRU 语义，不加入全部分支浏览器、Apply 2.0 或无消费者的 Wing 通用抽象。

## 验证和交接

- [x] 定向单元测试与消费者契约测试通过；生成规则变更同时验证旧缓存失效。
- [x] Auto typecheck、架构及全量测试；Wing 受影响包测试及构建。文档分类/链接在最终归档时复验。
- [x] 通过受控 `pnpm ext:dev:prepare` 和 metafile 来源检查；本地 Wing 联调通过，不表示 Registry 已发布。
- [x] Preview Git Right 与 Combo 的已执行交互、宽度与主题证据已记录；真实 JSON 标签通过 Extension Host。未验的 Windows/辅助技术及完整视觉矩阵见下方。
- [x] 点检文档中的敏感信息、当前/历史状态和剩余 TODO；按实际分支、版本及 dirty 状态进行本地提交/归档，不包含无关改动。

实现与测试完成后逐项登记证据；计划勾选不替代运行结果，外部发布和人工验收不得提前标为完成。

## 首批验证证据（历史阶段，2026-09-10）

- Node `22.23.1` / pnpm `10.15.1`，最终双方干净冻结源码的受控 `pnpm ext:dev:prepare` 通过；Auto **250 文件 / 1802 测试**，含启用本机 CMake 的测试，无跳过。类型检查通过，metafile 确认六个 Auto Wing 包来自并列候选，consumer node_modules 命中 **0**。CAD 依其独立 wrapper 验证原 Registry 基线（14 文件 / 50 测试），不将其称为 CAD 本地 Wing 消费。
- 架构门禁：262 个源码文件、27 个 pure graph、25 个 View root 通过。
- Wing `code-core`：20 文件 / 111 测试；`kt-codegen`：34 文件 / 158 测试。构造函数新增 10 例，Combo 13 例；类型检查与构建通过。
- 真实 VS Code `1.136.2` Extension Host 在隔离临时夹具中验证 **16 个流程**。包括同名 JSON 文件名标签、同 URI 复用、标题更新隔离，以及旧生成缓存拒绝、冲突/应用/保存/回滚、头文件服务和项目改名取消。未读写用户工程。
- Git Preview：实际点击验证中段选择/预检、分支取消和确认、dirty/occupied 对话框、分页、remote 详情；共享脚本执行测试补齐真实事件绑定。浏览器发现的父 CSP 阻断已使用每次随机 nonce 修复，未增加脚本 unsafe-inline 或 iframe 同源权限。
- 浏览器最后修正：Right 切换成功后，Primary 的分支、HEAD、简报同步；关闭重开仍为 `topic/demo`，只出现一组样例节点，选择归零。失败/取消、不同当前仓库及迟到回调隔离由新增集成回归覆盖。
- 构造函数真实生成结果在 macOS clang-format `23.1.0`、工程样式哈希下完成两组夹具 × LF/CRLF，边界与 expected 一致且二次格式化稳定。详见[缺陷与复验记录](bug/自动代码-构造函数结束标记缩进.md)。
- 增量源码敏感信息和范围审查：未发现新增密钥、带凭据 URL 或个人主目录路径进入源码；未开启两项排除功能，未修改外层 Shell。制品另行检查，不用源码审查替代。
- Wing 版本候选核验：12 包 release matrix、聚合构建与 87 文件 dist、1 个聚合包 + 10 个小包 tarball、隔离 npm consumer/SQLite/UI 单例/Vite build 均通过；packed manifest 无 workspace/file/link。97 份文档在标准并列拓扑下链接通过。仅本地候选，不是 npm 发布回执。

## 保留的验收边界

- Windows 实际 clang-format 版本/配置与 CAA 运行：未执行；不以 macOS 通过代替。
- 屏幕阅读器按用户要求不纳入本轮。系统强制颜色、多窗口及完整缩放矩阵，Git 完整键盘/拖动和极窄宿主矩阵：未逐项验收；已有自动测试与浏览器局部证据不冒充全平台通过。
- Auto 的旧精确 Registry 依赖保持不变；新版 Wing scoped 包公开后，仍需另行升级 manifest/lockfile 并执行干净 Registry/Marketplace 门禁。本地包只供内测。

## 首批本地冻结与制品（历史记录）

- Auto 源码：`codex/v0.9.2@72ed85dc5b772ae191a937cfe965cc4730c501be`，tree `994cc263d5cbc64661a3ae4827ba346dc04cee3c`；主体提交 `0807aeaec2d9cac8918318e6dfa0c5b5afa76768`，后续提交只修正 DOM 测试类型与文件尾空白。最终 prepare、Host 测试和打包时 Auto / Wing 源码均干净。
- Wing 源码：`v0.7.4@d14dc9ec99d6ede230c0ca109c2e7af11e5ba4bf`，版本和 tree 见上方。受控并列链接指向该候选，不使用主库 `develop` 代替。
- 本地内测包：`dist/vsix/0.9.2-internal-20260910/kt-auto-code-0.9.2.vsix`，**66 文件 / 861303 字节**；SHA-256 **`86b5040919097b6d5fab7f2d8ea1406adceec2ad6cb154bf8aa993505bb5fca3`**。`ext:package:local-wing` 与 `ext:verify-artifact --local-wing` 通过，后者实际校验规则 `1.0.1` 的生成边界。
- 同目录保留两仓冻结源码 tar.gz、完整 prepare 日志、VSIX 哈希与 `publishable:false` 来源回执、真实 Host 16 流程回执、浏览器局部点检、敏感信息扫描和 `freeze.json`。包内源码不包含之后补充的归档文档提交。
- 包内 66 个文件扫描未发现私钥、GitHub/AWS 凭据或带密码 URL；只有 `extension/dist/extension.js` 保留已知本地 Wing 绝对路径来源标识，未发现其他个人主目录路径。**这不是可公开外发的脱敏包**；归档日志也含本机路径，仅在本地保存。
- 冻结时主目录为 `master` / `0.9.0`，当时的 15 个发布准备改动未混入本轮冻结提交。2026-09-10 用户取消发布后，已备份并撤回主库准备改动；有用的日期分页测试及取消发布说明归入开发 worktree。原 0.9.1 冻结分支及内测包不变。
- 本轮未推送、未建 tag、未 npm publish 或上传 Marketplace；清理对话框与编译工具后续仍不开启。

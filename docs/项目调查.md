# KT Auto Code 项目调查

> 初次调查：2026-07-11；当前状态复核：2026-08-27
> 调查范围：根目录 README、`docs/`、核心源码、CLI、VS Code 扩展和测试。

## 结论

KT Auto Code 是一套面向 **CAA / MSVC C++** 工作流的源码编码治理工具。它针对中文 Windows（CP936 / GBK）与跨国协作中常见的代码页差异，扫描并修正可能引发 **MSVC C4819**、乱码或跨平台解释不一致的文件。

项目当前不是纯规划或原型：核心工具、CLI、KT Auto Code VS Code 扩展和自动化测试均已落地。根扩展当前 Windows 测试候选为 `0.8.0`，仓库保持 `private`；本仓只交付 KT Auto Code VSIX，CAD 扩展已迁往并列 `kt-auto-cad` 仓库独立发布。

## 解决的问题

CAA 工程的历史工具链经常依赖本机代码页。头文件中一旦混入 Word / PDF 粘贴产生的 Windows-1252 弯引号、NBSP、全角标点、GBK 中文或 BOM，同一批字节在不同国家和不同系统代码页上可能得到不同解释。

项目将问题拆成两个层次：

| 工具 | 处理层次 | 当前能力 | 默认目标 |
| --- | --- | --- | --- |
| `headerAscii` | 文件内容的字节级清理 | 识别并替换 CP1252 标点、NBSP、全角标点及其他非 ASCII 内容；识别 BOM | 头文件保持纯 ASCII |
| `encodingFix` | 整个文件的编码检测与转换 | 识别 ASCII、GBK、UTF-8、UTF-8 BOM、UTF-16、UTF-32 和未知编码 | UTF-8 无 BOM |

两者不是重复功能：`encodingFix` 回答“整个文件是什么编码、是否需要转码”，`headerAscii` 回答“文件内容中有哪些不应出现的字节或字符”。

字符映射、扫描扩展名和 BOM 行为的完整规则不在本文重复，分别以[源文件编码扫描](./源文件编码扫描.md)和[编码修正](./编码修正.md)为准。

## 实际使用形态

项目提供 CLI 与 VS Code 两类入口。编码等 Auto 专属 Host-neutral 逻辑位于 `src/core/`，跨宿主公共算法与契约以 Phoenix Wing 为真源；扩展通过 adapter 组合两者，不维护第二套 Wing 算法：

```text
CLI scripts ─────────────> src/core/ Auto Host-neutral 逻辑 ──> 目标工程文件
VS Code src/ Host ──┬────> src/core/ Auto 模型与工作流
                    └────> Phoenix Wing 公共算法与契约
```

### CLI

- `scan-encoding`：扫描源文件中的问题字节。
- `fix-headers`：将头文件清理为纯 ASCII，会写回文件。
- `scan-file-encoding`：预检整文件编码。
- `convert-file-encoding`：将可转换文件写为 UTF-8 无 BOM。

CLI 的完整参数和示例见[源文件编码扫描](./源文件编码扫描.md)与[编码修正](./编码修正.md)。

### VS Code / Cursor 扩展

扩展在 Activity Bar 提供一个 **KT Auto Code** 入口，以 Ribbon + 当前 Module Block 组织编码、Ignore、搜索替换、成员排序、UUID、CAA UI、Codegen、Git 与 Run 等功能。完整清单以[产品功能归属矩阵](./产品功能归属矩阵.md)为准。

扩展负责工作区权限、VS Code API、交互、确认、结果展示和打开文件；领域逻辑来自 `src/core/` 或 Phoenix Wing。仓库根是唯一扩展包根，manifest、源码、资源、构建和发布不再经过嵌套 `extension/` workspace。

## 代码结构与职责

| 目录 | 职责 |
| --- | --- |
| 根 `package.json` | 唯一扩展 manifest、版本及开发/验证/发布入口 |
| `src/core/` | 无 VS Code 依赖的 Auto 模型、工作流、扫描、转码、Ignore、Git/Run ViewModel |
| `src/` 其余目录 | VS Code Extension Host、工具 adapter、Primary/Webview 与宿主测试 |
| `media/`、`resources/` | 扩展图标及随 VSIX 发布的只读 runner |
| `scripts/` | CLI，以及 Wing 联调、构建、Host smoke、打包和发布门禁 |
| `tests/fixtures/` | GBK、UTF-8 BOM、UTF-16、Codegen 与 Host smoke 等回归样例 |
| `tests/webview/` | 可在浏览器中复核的 Webview 结构夹具 |
| `docs/` | 当前约束、专项计划与 `历史/` 归档 |

核心实现分工如下：

- `sourceEncodingScan.ts` / `sourceEncodingWalk.ts`：字节级扫描、ASCII 清理和目录遍历。
- `fileEncoding.ts` / `fileEncodingWalk.ts`：整文件编码检测、期望判断和 UTF-8 无 BOM 转换。
- `fileBom.ts`：BOM 识别及 UTF-16 等文件的安全转换。
- `scanScope.ts`：头文件、源文件、Markdown 范围及默认跳过目录。
- `dotIgnore.ts`：读取 `.phoenix/.ignore`，也支持从 `.gitignore` 同步规则。
- `src/tools/`：工具注册表及 `headerAscii`、`encodingFix` 的插件适配层。

## 当前实现状态

调查时确认的状态：

| 项目 | 状态 |
| --- | --- |
| 字节级扫描与修正 | 已实现 |
| 整文件编码检测与转换 | 已实现 |
| CLI | 已实现 |
| VS Code / Cursor 基础插件 | 已实现 |
| KT Auto CAD 依赖插件 | 由并列 `kt-auto-cad` 仓库独立维护；跨仓联调仍由 `pnpm dev` 支持 |
| 扫描范围配置 | 已实现 |
| `.phoenix/.ignore` | 已实现 |
| 自动测试 | 由 `pnpm verify:ci` 在当前 HEAD 执行，不在文档手写易漂移计数 |
| 扩展构建验证 | Auto 在本仓独立验证；CAD 在其仓库独立验证 |
| VSIX 打包 | 两个仓库分别产出并校验自己的 VSIX |
| Extension Host 手工验证 | 待执行 |

仓库根是单一 pnpm 包，执行一次 `pnpm install` 即可。跨仓双插件 F5 要求并列 `../kt-auto-cad`，两个仓库分别安装依赖和打包，步骤见[开发与测试](./开发与测试.md)。

## 行为边界与注意事项

1. 这是编码和字节治理工具，不理解 C++ 语法。它不会判断被替换的中文是否位于注释、字符串字面量或其他语义位置。
2. `fix-headers` 和编码转换会直接写回目标文件。插件提供确认框，CLI 调用者仍应先预检，并通过 Git diff 审查结果。
3. GBK 检测采用“UTF-8 严格解码失败后，字节流符合 GBK 双字节规则”的策略；它适合目标场景，但不是通用的编码概率识别器。
4. UTF-32 和未知编码当前仅报告，不自动转换。
5. ASCII 同时也是合法的 UTF-8 无 BOM，因此整文件编码工具会把纯 ASCII 判为符合目标。
6. 扫描会跳过常见构建目录和隐藏目录；工作区还可用 `.phoenix/.ignore` 缩小范围。

## 文档职责

本文作为项目的高层调查和现状入口，其余文档保留专项细节：

| 文档 | 负责内容 |
| --- | --- |
| [源文件编码扫描](./源文件编码扫描.md) | 字节级规则、字符映射表、扫描范围和 CLI 示例 |
| [编码修正](./编码修正.md) | 编码检测策略、转换方向和期望编码规则 |
| [仓库结构与扁平化迁移计划](./仓库结构与扁平化迁移计划.md) | 根包边界、迁移决策和完成证据 |
| [早期 VS Code 插件规划](./历史/vscode插件规划.md) | 初始工具注册与 UI 设计背景，仅作历史参考 |
| [开发与测试](./开发与测试.md) | 安装、测试、F5、配置项、检查清单和打包 |

# 0.9.0 Primary 与 Editor 联动及统一工具运行时计划

状态：三段式外壳为现行锁定契约；统一工具运行时与内部热预览继续迭代

Owner：KT Auto Code maintainers

适用版本：目标 0.9.0

最后核验：2026-09-06

## 1. 分支与本轮边界

- `v0.9.0` 从 `9c0ceba7d112b0c8d3a859c69760eb9d18abf512` 开始，用于本轮 Primary 布局讨论、快速原型、两个 Editor View 的可行性验证，以及后续统一工具运行时的分阶段开发。
- 先前完成的 Ignore 添加查重、“修正”与显式保存已隔离到标准 worktree 根下的 `kt-auto-code-ignore-fix`，分支为 `fix/ignore-target-correction`；保持不合并、不推送，也不以 cherry-pick、rebase 等方式带入本原型，`v0.9.0` 不依赖这批文件。
- 原型首先验证 Shell 层级和统一工具激活；界面可用后，再用**项目改名**与**自动编译**两个复杂 View 验证 Primary / Editor 分工。
- 原型不更改领域算法、改名写盘门禁或编译脚本语义；扩展清单已对齐目标版本 0.9.0，但本轮不打包、不发布、不推送。

### 1.1 与并行 0.8.x 的边界

- `0.8.x` 继续作为小步发布线，可做缺陷修复、单个 Tool/Editor View 内部样式与领域能力的小改进；尽量不修改导航 Tree、Group/Tool 语义、Action 注册、Primary 外壳和跨 Tool 生命周期。
- `0.9.0` 独占 Navigation Schema、Tool Registry、Action Router、通用 Tool Navigator、Primary/Editor 统一运行时及相关兼容清理；这些变化不反向下放到 `0.8.x`。
- 两条线都可能触及 `package.json`、CHANGELOG、`tools/types.ts`、`sidebarViewProvider.ts` 或 `panelHtml.ts`。`0.8.x` 如确需修改共享文件，应保持 Tool 局部、附独立测试、避免格式化整文件，也不得顺手调整 Tree/Shell；这样后续可以按语义合并，而不是整文件选边。
- `v0.9.0` 保持当前起点和已归档节点，不因另一条线每天前进而自动 rebase、merge 或 cherry-pick。待某个 `0.8.x` 发布节点稳定后，先只读比较 merge-base、共享文件与测试，再由用户明确选择一次性合入策略。
- Ignore fix 分支仍保持独立；无论 `0.8.x` 或 `0.9.0` 都不得因名字相近自动带入。

## 2. 已确认目标 Shell：三个一级区域

```text
KT Auto Code                                      [忽略] [设置]
├─ Toolbar Strip：`箭头 + Ribbon + …` 共用一层；无独立标题行
├─ 目录 Block：固定一行，只表达共享目录
└─ Current Tool Block
   ├─ 紧凑 Header：折叠箭头 + 当前工具标题 + `×`
   └─ Body：操作、状态和结果；唯一日常纵向滚动区
```

这是当前唯一现行外壳契约：Webview 恰好包含 Toolbar Strip、固定目录、Current Tool Block 三个顺序不可交换的一级区域。早期讨论过的“两区 Shell”“目录显隐”“无 Header Tool Surface”和“右上悬浮 `×`”均已废弃，只能作为历史背景，不得指导实现或验收。Ignore 与 Settings 是同级逻辑工具，进入第三段 Current Tool Block，不新增第四段。

### 2.1 原生 Header 与固定目录

- 原生 View Header 固定依次放 Ignore、Settings，不放搜索放大镜、目录显隐/齿轮或 Webview 内部 Ignore 快捷方式。
- 当前两项是 Header 已确认的最小核心集；以后只有真正全局且高频的动作才可通过显式评审加入，且必须同步契约和回归测试。
- 目录是 Webview 第二个一级区域，固定显示一行；无折叠箭头、无 Body、无显隐状态。
- 只表达共享工作目录；不放工具标题、MRU、任务状态或操作按钮。
- 目录路径占用剩余宽度，完整值保留在 Tooltip 和 `aria-label`。
- 行内保留右侧唯一实际目录选择入口，不再用左侧装饰性文件夹，也不增加目录齿轮。

### 2.2 Toolbar Strip

- 箭头、Ribbon 和唯一 `…` 在同一行，不再为“工具栏”额外浪费一行标题。
- 展开态保留现在的图标+文字 Ribbon；紧凑态只显示图标。
- Toolbar 箭头只改变 Ribbon 的展开/紧凑形态，不折叠 Current Tool Block 或目录，不销毁工具状态。
- 展开与紧凑必须复用同一个 Ribbon DOM；`…` 仍是 Ribbon 定制的唯一入口。
- 紧凑态中因宽度不可见的入口必须仍能从 `…` 到达；`…` 不是装饰按钮。
- 现有展开/紧凑效果已获用户确认并作为本轮固定基线；更多二级菜单如何迁入统一直接叶子模型留作后续研究，不继续扩大本次阶段归档。

### 2.3 Current Tool Block

- 只容纳当前一个逻辑工具，不堆叠多个并列业务 Block。
- 紧凑 Header 必须显示当前工具标题，并同时提供独立折叠动作与明确的 `×`。折叠只控制 Body 可见性；`×` 只关闭逻辑工具，两者不得共用一个动作或状态。
- 从 Ribbon、`…`、命令、原生 Header 或 Editor 激活工具时，统一更新当前工具与 MRU；折叠状态按 Shell 规则恢复，不得靠移除 Header 模拟折叠。
- `×` 关闭后按同一个 MRU 栈恢复上一个逻辑工具；最后一个关闭后显示欢迎态。Editor、后台扫描/进程、草稿和结果仍按各自生命周期保留。
- Current Tool Block 填充剩余高度；Primary 页面、Toolbar、目录和 Current Tool Header 都不做日常纵向滚动，只有 Body 是唯一滚动边界。

```text
Ribbon 展开 + Current Tool Body 展开
原生 Header                                             [忽略] [设置]
├─ ⌄ [代码辅助] [Git] [Run] [搜索替换]                       […]
├─ 目录 [当前目录……………………………………………… ▾] [选择目录]
└─ ⌄ 搜索替换                                                 [×]
      [搜索] [替换] [项目改名]
      状态与结果（Body 内滚动）

Ribbon 紧凑 + Current Tool Body 折叠
原生 Header                                             [忽略] [设置]
├─ › [当前图标] [图标] [图标] [图标]                         […]
├─ 目录 [当前目录……………………………………………… ▾] [选择目录]
└─ › 搜索替换                                                 [×]
```

## 3. 一级/二级入口是同一种工具

“一级”和“二级”只表示按钮放在哪个导航位置，不表示两种不同的 Primary View：

- Ribbon 上的“搜索替换”和功能菜单里的“头文件引用修正”都是普通 `toolId`。
- Ribbon、菜单、命令和 Editor 只能把已注册的直接叶子 `toolId` 交给统一激活通道，并在同一 Current Tool Block 中渲染；聚合目录 ID 不得代替叶子执行。
- 工具的状态、MRU、Current Tool Body 折叠、关闭、草稿恢复和权限判断不因入口层级而分叉。
- “功能目录”只是为不常用工具提供更深导航；它不创建另一套二级工具运行时。

建议统一描述：

```ts
interface KtcToolDescriptor {
  readonly id: string;
  readonly title: string;
  readonly entry: {
    readonly ribbon?: { readonly order: number };
    readonly menu?: { readonly group: string; readonly order: number };
  };
}

interface KtcToolActivation {
  readonly toolId: string;
  readonly source: "ribbon" | "menu" | "editor" | "command";
  readonly preserveFocus?: boolean;
}
```

`source` 只用于焦点和诊断，不决定工具类型。系统始终通过 `toolId` 选择当前 Current Tool Block 内容。

## 4. 简单工具与复杂工具的统一分工

| 工具形态 | Primary Current Tool Block | 右侧 Editor View |
| --- | --- | --- |
| 简单工具 | 表单、操作、状态和短结果全部保留 | 不需要 |
| 复杂工具 | 当前任务摘要、计数、阶段状态、少量高频动作、“回到 View” | 完整表单、配置、长列表、规则、详细结果和日志 |

同一复杂工具仍只有一个领域 Controller 和一份真实状态。Primary 与 Editor 只是两个投影，不得互相模拟 DOM 点击，也不得各自保存一份可执行草稿。

## 5. Editor companion 契约

```ts
interface KtcEditorPrimaryContribution {
  readonly panelId: string;
  readonly toolId: "projectRename" | "autoBuild";
  readonly sessionId: string;
  readonly revision: number;
  readonly lifecycle: "active" | "visible" | "open-inactive" | "disposed";
  readonly title: string;
  readonly ready: boolean;
  readonly status: "idle" | "running" | "done" | "error";
  readonly message: string;
  readonly summary: readonly { label: string; value: string }[];
  readonly actions: readonly {
    id: string;
    label: string;
    enabled: boolean;
    tone?: "primary" | "secondary" | "danger";
    disabledReason?: string;
  }[];
}
```

Extension Host coordinator 保存快照并把它投影到当前 Current Tool Block。Primary 发送 `panelId + toolId + sessionId + revision + actionId`；文件系统、进程、报告和写盘继续由 Controller/Host 负责。

### 5.1 激活与焦点

1. Ribbon、二级菜单、命令面板和 Editor 的直接叶子激活都进入同一 `activateTool` 通道；`source` 只影响焦点保持和诊断。
2. Editor `active=true` 时使对应工具成为当前 Current Tool Block，但用 preserve-focus 路径保持焦点在 Editor。
3. 用户点击 Primary 后 Editor 会暂时变为 inactive：仍在分栏中可见则标记 `visible`，被其他 Tab 遮住才标记 `open-inactive`；两者都不立即切回旧工具。
4. 分栏同时可见多个 Editor 时，以最近一次 `active=true` 的 Panel 为准。
5. Panel dispose 后将路由标记为 `disposed` 并停止转发；可以保留拒绝迟到消息所需的最小 tombstone，但旧 session 不再可执行。
6. 用户手工隐藏侧栏后，Editor 切换只更新 Host 状态，不反复强制打开 Primary。

### 5.2 草稿与危险动作

自动编译的未保存配置和项目改名的“表单已修改 / 报告已失效”仍有 Editor DOM 状态。Primary 不能使用旧 `workspaceState` 或上一次摘要直接执行。

需要最新草稿的动作统一使用握手：

```text
Primary 点击
  → Host 校验 toolId / sessionId
  → Editor 返回 draftRevision / reportId / 完整表单
  → Host 再次校验 session 和 revision
  → 进入既有 Controller 门禁
```

Editor 未 ready、已关闭、请求超时、revision 过期或报告漂移时都不执行。

## 6. 两个原型验证对象

| 工具 | Primary Current Tool Block 投影 | Editor 继续保留 |
| --- | --- | --- |
| 项目改名 | 绑定目录、`Source → Target`、规则/命中/风险数、阶段状态；首先接“回到 View、取消、Git 对比” | 名称和前缀输入、历史、档案、规则、候选、命中与风险列表、Diff、写盘门禁 |
| 自动编译 | 配置名、工作目录、项目数、平台、进度和失败数；首先接“回到 View、停止、Output” | Root/3rdParty/分支配置、项目表、预检、脚本、清理选项和完整日志 |

首阶段只镜像不依赖草稿的安全动作，并保留 Editor 原按钮，避免用户隐藏 Primary 后无法完成任务。握手稳定后才候选迁移“分析 / 启动 / 预览 Diff / 执行改名”。

相关已实现行为与安全边界分别以[项目改名 View 整改方案](./项目改名View整改方案.md)和[编译工具说明](./编译工具/README.md)为基线。本讨论稿只验证 Shell、状态投影和入口分工；出现冲突时，不得用原型降低这两份文档已经定义的执行门禁。

## 7. 已知实施风险

- 现行三段式与前一版“两区、目录显隐、无 Header、悬浮关闭”原型存在 DOM 和状态差异。迁移必须删除旧状态和旧入口，不能同时保留两套外壳；旧方案只作为迁移输入，不再作为验收目标。
- 改变 DOM 层级会影响边框、Tab 顺序、焦点恢复、折叠状态、MRU 与唯一滚动区，必须作为独立 Shell 步骤验证。
- Current Tool Header 的折叠与 `×` 是两个独立动作；若继续复用旧的“重复点击 Ribbon 折叠”或浮动关闭处理，会产生冲突状态和键盘焦点回归。
- Auto Build 手工添加目前没有真正以 Git top-level 作为项目身份，同一仓库的根目录/子目录可能重复。这是独立功能缺陷，记入 TODO，不与 Shell 原型混成一个修改。
- 项目改名写盘中需禁止新任务替换旧 session，防止迟到结果覆盖新状态。原型不降低该安全门禁。
- 现有功能目录仍有可继续迁移的二级入口。后续研究应逐项确认入口身份、状态所有权和 Primary/Editor 分工；本轮只证明统一直接叶子通道可行，不批量搬迁剩余菜单。

## 8. 实施拆分

1. [x] **分支隔离**：Ignore 前序修正存入 `fix/ignore-target-correction`，不合并。
2. [x] **历史原型归档**：曾验证“两区、目录显隐、无 Header、悬浮关闭”方案；该结果只证明统一激活与 MRU 可行，外壳方案现已废弃。
3. [ ] **三段 Shell 迁移**：Header 收敛为 Ignore → Settings；DOM 改为 Toolbar → 固定目录 → Current Tool Block；Current Tool Header 接入独立折叠与 `×`，Body 成为唯一滚动区。
4. [x] **统一工具激活**：原型源码已让 Ribbon / 功能菜单 / 命令 / Editor 复用直接叶子 `toolId` 的激活、MRU 和关闭通道；聚焦自动测试通过。
5. [x] **项目改名 companion**：原型已接只读摘要、回到 View、取消和 Git 对比，并有 Controller/注册测试及实际 Editor→Primary 联动点检。
6. [x] **自动编译 companion**：原型已接运行摘要、回到 View、停止和 Output，并有 Controller/注册测试及 macOS 检查模式实际联动点检；真实 Windows CAA 进程仍是发布前平台点检项。
7. [x] **历史点检与本地节点**：旧外壳原型曾完成自动门禁和核心人工观感，并随本地节点归档；该结果不覆盖第 3 项待完成的现行三段 Shell 验收。

历史验收证据（2026-09-06，仅供迁移回归参考）：旧原型当时通过 170 个测试文件、958 项测试（另有 1 项既有跳过），并完成类型、文档、架构、构建和本地 Wing 门禁；其统一叶子激活、MRU 与 Editor 保持行为可继续复用。旧原型的“两区、目录显隐、无 Header、悬浮 `×`”视觉和交互结论已废弃，不得据此勾选现行三段式验收项。

## 9. 自动测试计划

- DOM 恰好包含 Toolbar Strip、固定目录、Current Tool Block 三个顺序固定的一级区域；Ignore/Settings 不生成第四段。
- 原生 Header 的可见动作顺序为 Ignore、Settings；不存在目录显隐/齿轮、搜索放大镜或 Webview 内部 Ignore 快捷入口。
- 目录固定单行、无折叠箭头和 Body；左侧无装饰文件夹，右侧唯一文件夹按钮可达。
- Toolbar 紧凑/展开不影响目录和 Current Tool；中间始终是一份 Ribbon，右侧始终只有一个可用 `…`，紧凑态隐藏项可从菜单到达。
- Current Tool Header 的折叠与 `×` 有独立状态和事件；折叠只控制 Body，关闭只更新逻辑工具/MRU，不影响 Editor 或任务。
- Toolbar、目录、Current Tool Header 与 Body 的 Tab 顺序可预测；不存在空 Header 焦点位。
- Ribbon、二级菜单、命令和 Editor 的同一直接叶子 `toolId` 产生完全相同的当前工具、MRU、草稿恢复和 `×` 行为。
- Editor active/inactive/visible/disposed 与双分栏最近激活归约有纯状态测试。
- Primary 每条 companion 动作都校验 `toolId/sessionId/actionId/ready`；迟到消息不能作用于新任务。
- 项目改名执行仍经过冻结报告和漂移复验；编译不从旧 `workspaceState` 启动。
- 280 / 360 / 560 / 760px 宽度下无水平页面滚动，目录选择、当前 Ribbon 项、Current Tool 折叠与 `×` 始终可达且互不遮挡。

### 9.1 自动门禁执行顺序

1. 先运行 companion 状态模型、项目改名 Controller、自动编译 Controller、Primary Provider 和 HTML 契约的定向测试。
2. 再运行 `pnpm typecheck` 与 `pnpm test`，确保简单 Primary 工具和既有 Editor View 没有回归。
3. 运行 `pnpm docs:check`，确认本文、索引和生成清单一致且当前文档链接有效。
4. 用 `pnpm ext:dev:code` 启动未安装模式的 Code Extension Development Host 做界面点检；最终本地 Wing 集成声明必须以 `pnpm ext:dev:prepare` 成功为准。
5. 每次门禁记录命令、退出码和通过/失败摘要即可；不得把本机绝对路径、完整命令环境、项目源文件内容或私有配置写入归档证据。

## 10. 人工验收点

- [ ] 深色、浅色和高对比主题下，Toolbar、目录、Current Tool 三段层级清楚且连续，没有旧两区嵌套或第四个正式一级区域。
- [ ] Header 只依次显示 Ignore、Settings；没有目录显隐/齿轮、搜索放大镜或 Webview 内部 Ignore 快捷入口。
- [ ] 目录始终固定一行，无折叠箭头或 Body；左侧没有装饰性文件夹，右侧实际选择按钮不重复。
- [ ] Toolbar 无独立标题行；展开/紧凑复用同一 Ribbon DOM，`…` 始终只有一个。
- [ ] 分别从 Ribbon、功能目录、命令和 Editor 打开同一叶子工具，都在同一 Current Tool Block 显示，没有两套 Header、关闭或状态。
- [ ] Current Tool Header 折叠只隐藏 Body；再次展开恢复相同表单、结果和滚动位置，不改变 MRU、Editor 或任务。
- [ ] 简单工具可在 Primary 独立完成；复杂工具的 Primary 只留摘要与阶段动作，长列表只在 Editor。
- [ ] 打开项目改名后 Primary 跟随但焦点仍在 Editor；取消、Git 对比和回到 View 只执行一次。
- [ ] 打开自动编译后 Primary 不复制项目表；停止、Output 与运行计数同步。
- [ ] 在两个 Editor 分栏之间切换，Primary 始终跟随最近激活工具；点击 Primary 本身不会使 companion 立即消失。
- [ ] Current Tool Header 的 `×` 仅关闭左侧逻辑工具，不关闭 Editor、不停止进程、不执行改名或清空结果；窄宽时与折叠动作、标题互不遮挡。

### 10.1 点检准备与证据口径

- 只使用可删除的合成 Git fixture，不用真实客户仓库、个人目录名、凭据或私有源码。以下 `RENAME_FIXTURE`、`BUILD_FIXTURE` 只是测试目录代号，不代表要写入文档或日志的绝对路径。
- 项目改名 fixture 至少包含 `DemoOpenIssue`、`demo-open-issue`、`demo_open_issue`、`DEMO_OPEN_ISSUE`，以及 `PNX` / `pnx` 前缀、一个文件名命中、一个目录名命中、一个被 Ignore 排除的命中和一个故意制造的目标冲突。
- 自动编译 fixture 至少包含两个无副作用的示例项目、一份可保存/恢复的配置和一个可控的长运行任务；Windows 实跑使用获准的测试项目，不能把真实 Root、3rdParty 或 CAA 安装位置写入测试报告。
- 每项记录“步骤、预期、实际、通过/失败、必要截图编号”。截图应裁掉路径、账号、仓库远端、终端环境和源文件正文。

### 10.2 项目改名：Primary / Editor 分工

- [ ] 在 Primary 搜索替换中填写 Source、Target 和启用的显式变形规则，点击“项目改名”；Editor 获得焦点，绑定当前目录且正确带入草稿，但不会自动分析或写盘。
- [ ] Editor ready 后，Primary Current Tool Block 显示同一 session 的 `Source → Target`、规则数、命中数、风险数和阶段状态；不复制完整规则表、文件正文或长结果列表。
- [ ] 点击“分析”只生成只读报告；被 Ignore 排除的命中不进入候选，冲突项进入风险而非可执行计划。
- [ ] 修改 Editor 中名称、前缀、规则值或启用状态后，旧报告立即失效；旧 revision 的 Primary 动作被拒绝，不得执行旧计划。
- [ ] Primary 的“回到 View”只 reveal 既有 Panel；“取消”只取消当前扫描；“Git 对比”在成功写盘前保持禁用，三者都只触发一次。
- [ ] 从 Editor 打开写盘前原生 Diff 时磁盘无变化；确认执行后再用原生 SCM/Git diff 验证实际改动，且插件不会自动暂存或提交。
- [ ] 结束任务或关闭 Editor 后再新建任务，session 必须变化；上一 session 的迟到分析结果、取消或 Git 动作均被丢弃。

### 10.3 自动编译：macOS 盲开发点检

- [ ] 用 `pnpm ext:dev:code` 打开开发 Host，自动编译 View 在 macOS 上仍可打开并完整显示 Root、3rdParty、项目表、预检、脚本和日志区域，不因平台而隐藏入口。
- [ ] 使用 `BUILD_FIXTURE` 的 POSIX 目录完成编辑、目录/Git 探测、预检、配置保存/恢复和 PS1 生成；这些结果只证明界面与规划链路可开发，不证明 CAA/MSVC 编译成功。
- [ ] 载入含 Windows 盘符或 UNC 的合成配置时只保留计划，不访问或改写本机同名路径，不直接同步 Root；导出脚本必须重新选择本机目标。
- [ ] 点击“运行”时给出明确平台限制；不得伪造成功结果、不得调用缺失的 Windows/CAA 工具链。Primary 和 Editor 均显示一致的不可实跑原因。
- [ ] Primary 只显示配置名、工作目录摘要、项目数、平台、进度/失败数以及“回到 View、停止、Output”；项目表、分支配置、脚本正文和完整日志仍只在 Editor/Output。

### 10.4 自动编译：Windows 实跑点检

- [ ] 在 Windows PowerShell 5.1、可用 MSVC/CAA 环境和获准测试项目上重新执行；macOS 结果不得替代本项。
- [ ] 分别验证单项目与多项目运行、顺序/并行状态、成功/失败退出码、Output 跳转和日志归属；Primary 计数及终态应与 Editor/Output 一致。
- [ ] 长任务运行中从 Primary 点击“停止”，只向当前 session 发出一次停止请求；终态可解释，旧 session 的停止动作不能影响下一次运行。
- [ ] Root、共享根、相对路径、错误文件类型以及包含 junction/reparse point 的危险清理目标，在 Git/CMake/删除副作用前被拒绝。
- [ ] PS1 同步/导出后在 Windows 端重新校验完整配置；任何写脚本成功提示不得等同于实际编译通过。

### 10.5 双 Editor 生命周期

- [ ] 左右分栏同时打开“项目改名”和“自动编译”。先激活项目改名，Primary 显示 `projectRename`；再激活自动编译，Primary 切换为 `autoBuild`，Editor 焦点不被侧栏抢走。
- [ ] 从 Active Editor 点击 Primary 后，该 Editor 在分栏仍显示时进入 `visible`；切到同组其他 Tab 后进入 `open-inactive`。两种状态下当前 companion 都不立即退回旧工具，再次点击 Editor 可恢复 `active`，不会新建 session。
- [ ] 两个 Editor 交替激活至少三轮，Primary 始终使用最近一次 `active=true` 的 Panel；两个摘要、动作 token 和 revision 不串线。
- [ ] 关闭当前 Editor 后，其路由进入 `disposed`，Primary 显示可解释的已关闭状态或回退到 MRU；关闭另一个 Editor 不得误删当前 session。
- [ ] 重开同类 Editor 后得到新 session；模拟旧 Panel 的迟到状态和动作，Host 应拒绝且新任务状态保持不变。
- [ ] 手工隐藏 Primary 后继续切换两个 Editor，Host 可以更新快照，但不得反复强制展开侧栏。

### 10.6 仅关闭 Primary Current Tool Block

- [ ] 项目改名扫描或自动编译任务运行时点击 Current Tool Header 的 `×`；只关闭左侧逻辑工具并按 MRU/欢迎态回退，Editor、扫描/进程和结果均保持原状。
- [ ] `×` 不发送 cancel/stop/dispose，不清空 Editor 草稿、冻结报告、日志或工作区状态；Output/SCM 也不被关闭。
- [ ] 关闭 Primary 后，通过重新激活现有 Editor、Ribbon、功能菜单或命令入口恢复同一工具；仍使用最新 session/revision，不重复启动任务。
- [ ] Primary 已关闭期间任务到达终态，重新显示后一次性呈现最终快照；不得重放过期阶段，也不得因隐藏期间的状态更新自动执行动作。

### 10.7 隐私与最小数据面

- [x] companion 快照只携带渲染摘要、状态、计数和不透明 session/revision/action 标识；不携带文件正文、完整规则档案、脚本正文、环境变量、凭据或完整进程命令。
- [x] Source/Target、目录等用户可见信息只在当前本地 UI 按功能需要展示；默认日志、测试快照、诊断、文档和提交说明不得复制这些值。可定位日志优先使用脱敏名称、工作区相对路径或不可逆 ID。
- [x] 未新增遥测或网络上报；Primary / Editor 同步只走 Extension Host 内部消息。`workspaceState` 仅保存必要的临时 UI/session 状态，不保存源文件内容、密钥或机器工具绝对路径。
- [x] 错误信息不回显完整消息载荷、配置 JSON、命令环境或源文件片段；未知/过期动作只记录模块、阶段和脱敏原因。
- [x] 提交前分别扫描工作树与暂存差异中的用户目录、邮箱、令牌/密钥形态、私有远端 URL 和测试正文；命中项逐条解释或移除，不能只因测试通过就视为隐私点检完成。
- [x] 检查生成物和截图没有意外带入本机路径或私有内容；构建缓存、日志和临时 fixture 不进入提交。

隐私复核记录（2026-09-06）：Primary 状态文案改为短枚举 allowlist，完整错误继续留在所属 Editor/Output；项目改名 companion 不再冗余携带原始目录、Source 或 Target 字段。工作树新增行与未跟踪源码均已扫描，本机用户名、真实邮箱、密钥/令牌和私有远端均无命中；测试中出现的 `/Users/example`、`example.com` 和 `synthetic-test-token-not-a-secret` 仅用于证明脱敏边界，均为显式合成 fixture。未跟踪清单中没有 VSIX、日志、截图、coverage 或 dist 产物。

### 10.8 已废弃外壳原型的历史证据

以下勾选项只记录前一版“两区/目录显隐/无 Header”原型曾验证过的可复用运行时，不代表现行三段式外壳已通过；外壳相关结论必须按第 9、10 节重新点检：

- [x] 旧原型验证过目录上下文在视图重建时不丢失；现行契约不再提供目录显隐。
- [x] 旧原型验证过 Toolbar 展开/紧凑不销毁工具状态；现行折叠入口改由 Current Tool Header 独立承担。
- [x] 从 Ribbon 和功能目录切换直接叶子后复用同一逻辑工具运行时；关闭后按 MRU 回退，没有遗留旧的代码辅助子功能状态。
- [x] 从 Primary 搜索替换携带已填 Source/Target 打开项目改名；Editor 保持焦点，Primary 显示同一 session 的目录、名称对、规则数和安全动作摘要。
- [x] 打开自动编译后，Primary 显示配置、目录、项目数、任务数和 `darwin（检查）` 平台摘要；Editor 明确说明 macOS 只用于盲开发检查。
- [x] 关闭项目改名和自动编译的 Primary 逻辑工具时，Editor 均继续保持打开，Primary 按 MRU 回退；现行 `×` 已迁入 Current Tool Header，需重新验证其布局和焦点。

## 11. 原型验收与后续决策

三段 Shell 是现行锁定目标，原型不再重新决定是否采用该层级；它需要用自动测试和人工点检回答三个实施问题：

1. Toolbar、固定目录和 Current Tool Header/Body 在实际主题、窄宽和重载后是否清晰，且只有 Body 滚动？
2. Ribbon、菜单、命令和 Editor 是否真正共用同一直接叶子激活运行时与 Primary Current Tool Block？
3. 复杂工具是否可以稳定地把阶段按钮留在 Primary，把表单和长列表留在 Editor？

既有自动测试与核心运行态点检已证明直接叶子运行时、MRU 和两个 companion 原型可行；它们不证明现行三段 Shell 已实现或通过。三段 DOM、Current Tool Header、主题/窄宽/重载和真实 Extension Host 必须重新点检；Windows CAA 实跑仍是发布前平台项。是否把分析、启动、Diff 和执行改名等依赖最新草稿的动作进一步迁入 Primary，仍需单独决定。

## 12. 本次归档后的迁移顺序

1. **先做入口清单，不批量搬界面**：逐项记录现有二级入口的直接 `toolId`、状态所有者、Primary/Editor 分工、危险动作和兼容状态。
2. **正式迁移简单叶子**：优先选择不依赖 Editor 草稿、长列表或后台进程的工具，验证 Ribbon/菜单/命令共用激活、MRU、折叠、恢复和关闭。
3. **再迁复杂工具的安全投影**：项目改名与自动编译继续只把摘要、回到 View、取消/停止、Git/Output 等安全动作投影到 Primary；完整表单、规则表、项目表和日志留在 Editor。
4. **最后迁依赖最新草稿的动作**：分析、启动、写盘前 Diff 和执行改名必须先完成 draft/revision 握手与失效测试，不能直接从缓存摘要执行。
5. **完成兼容清理和发布点检**：只有迁移批次达到行为等价后才删除旧聚合状态与重复渲染；随后覆盖浅色/高对比、窄宽、重载、双 Editor 和 Windows CAA 实跑。

每个批次独立归档并保持可回退；当前 Header、固定目录与 Ribbon 展开/紧凑效果不随二级菜单迁移重新设计。

## 13. Tool Navigator：导航层级与二级区域

本节定义 Toolbar 内、归属于当前 Ribbon Group 的框架级下级导航控件。它不属于 Current Tool Body，也不形成第五段。后续不再用“一级/二级 Block”同时指模块、入口、列表和业务内容，而使用稳定名词：

```text
Navigation Tree
└─ Group（role: module）             Code / CAD / 后续模块
   └─ Group（role: tool-group）      代码辅助 / Git / Run / 替换 / 自动代码
      └─ Group（role: category）     C++ 整理 / 文件工具 / CAA（可选）
         └─ Tool（叶子）             编译工具 / 头文件修正 / 编码修正 / UUID …

Tool Registry（不进入 Tree）
└─ toolId → descriptor + Action Map + Controller + 必需 Primary / 可选 Right Surface
```

- `C++ 整理`、`文件工具`、`CAA` 等只作为 Tool Navigator 内的视觉分类，不拥有任务状态，也不再计作一个导航级别。
- Ribbon 入口本身也是 `group | tool`：Group 只展开或收起下级 Navigator，不拥有 Current Tool、Right View 或 Open Item，也不发送 Tool 激活/业务动作。若该 Group 下已有打开的叶子，点击 Group 可把组内 MRU 叶子设为当前；若没有已打开叶子，则保留现有 Current Tool，当前也为空时保持 Welcome。Group ID 绝不能代替具体 `toolId`，也不得偷偷打开第一个子项。Tool 点击才发出 `activateTool(toolId)`。
- Ribbon 下方区域命名为 **Tool Navigator**。已确认原型只保留无标题的紧凑网格，并可通过 `Aa` 显示 / 隐藏短名称；列数由自身宽度响应，布局变化不改变选择、MRU、任务、权限或 Controller。旧“大纲 / 网格”双模式仅由 legacy 呈现暂时保留给正式 View，不能成为新原型的状态机。
- 只有 Tool 叶子拥有 `toolId`、运行状态和 Primary/Editor 分工。分析、修正、启动、停止等是 Tool 内 Action，不继续增加导航层级。
- 只有一个 Tool 的 Group 仍使用相同数据模型，但 Group 点击也不得自动创建该 Tool 的 View；只有显式选择叶子或回到已经打开的组内 MRU 叶子才会激活 Tool。

Tool 的显示身份由版本化注册 JSON 唯一提供：`title`、`shortTitle`、`description` 及语义图标随 `toolId` 解析。Primary、Right、Open Items、Navigator 和 Ribbon 不得分别声明或拼接显示标题，不加 Group、Module 或 `KT Auto Code` 前缀；需要完整归属时只写入 tooltip / `aria-label`。迁移期旧 snapshot 的 `title` 字段只作兼容输入并由 Host 忽略，不能回退为显示真源。

目标运行时统一为：

```ts
type KtcToolSurfaces =
  | {
      readonly primary: { readonly kind: "full"; readonly rendererId: string; readonly modelVersion: number };
      readonly right?: { readonly rendererId: string; readonly modelVersion: number };
    }
  | {
      readonly primary: { readonly kind: "companion"; readonly rendererId: string; readonly modelVersion: number };
      readonly right: { readonly rendererId: string; readonly modelVersion: number };
    };

interface KtcToolDescriptor {
  readonly moduleId: string;
  readonly groupId: string;
  readonly toolId: string;
  readonly instancePolicy: { readonly kind: "single" } | { readonly kind: "multiple" };
  readonly surfaces: KtcToolSurfaces;
}

interface KtcToolActionDefinition {
  readonly actionId: string;
  readonly label: string;
  readonly placement: readonly ("primary" | "right")[];
  readonly risk: "read" | "write" | "process" | "destructive";
}

interface KtcToolActionState {
  readonly toolId: string;
  readonly actionId: string;
  readonly enabled: boolean;
  readonly revision?: number;
}

interface KtcToolRegistration {
  readonly descriptor: KtcToolDescriptor;
  readonly actions: Readonly<Record<string, KtcToolActionDefinition>>;
  readonly controller: KtcToolController;
}

interface KtcToolInstanceRef {
  readonly toolId: string;
  readonly instanceId: string;
  readonly sessionId: string;
}
```

导航入口只有两种语义：Group 入口提交 `groupId` 选择导航上下文，Tool 入口提交叶子 `toolId` 激活工具。Ribbon、下级网格、`…`、命令与 Right View 都可以直接激活 Tool。Framework 统一管理 Group 选择、Tool 实例、可见性、MRU、Surface attach / detach、恢复和 Action 路由。工具自行提供领域 Controller、必需 Primary Surface Adapter 与可选 Right Surface Adapter；两侧同时存在时仍是同一个逻辑实例。Primary 内容复杂度和是否提供 Right 由消费者显式声明，Framework 不根据 DOM 或数据量推断。Action 的外壳可以统一，但写盘、编译、删除等领域校验仍必须由工具 Controller 执行。

Action 不进入导航 Tree，也不作为 Tree 的第三种节点。每个 Tool 在 TypeScript Registry 中维护一份以 `actionId` 为键的静态 Action Map，声明名称、Primary/Editor 投影位置与风险类型；Host 合并各 Tool 的 Map，建立只读全局索引并用 `(toolId, actionId)` 统一寻址和校验。运行时 `enabled`、revision、冻结报告及 handler 仍由所属 Tool Controller 提供，不能写进导航 JSON。Shell 的 `activate / collapse / close / reveal` 使用独立的系统 Action 命名空间，与 `analyze / apply / stop` 等业务 Action 分开注册，避免同名动作跨工具串线或绕过领域门禁。

导航采用“配置描述、代码实现”的双注册方式：

```json
{
  "schemaVersion": 1,
  "nodes": [{
    "kind": "group",
    "role": "module",
    "id": "code",
    "title": "Code",
    "children": [{
      "kind": "group",
      "role": "tool-group",
      "id": "codeAssistant",
      "title": "代码辅助",
      "children": [{
        "kind": "group",
        "role": "category",
        "id": "cpp",
        "title": "C++ 整理",
        "children": [
          { "kind": "tool", "toolId": "autoBuild" },
          { "kind": "tool", "toolId": "headerIncludeFix" }
        ]
      }]
    }]
  }]
}
```

- 模块随包 Manifest JSON 记录完整层级、标题、图标、顺序、默认叶子和允许的 Navigator 展示方式，不包含 Tool 的 Primary/Editor `presentation`、函数或任意可执行脚本。
- 用户布局状态只记录短名称显隐、固定、排序和展开偏好，并引用稳定 ID；不得复制或改写能力树。
- TypeScript Registry 按 `toolId` 注册 Controller（提供运行态与 Action handler）、ViewModel、`instancePolicy`、必需 Primary 与可选 Right Surface。Host 合并两者后，缺实现的叶子禁用并输出诊断，不允许从 JSON 动态执行代码。
- 导航节点使用递归可判别联合：Module / Tool Group / Category 在结构上都是 `kind: "group"`，只以 `role` 区分模块所有权、展示层级和默认样式；Group 只导航，不能声明可执行 `toolId`。Tool 叶子不能再有 `children`。Group 可声明 `defaultToolId`，但目标必须是自身后代叶子。
- 同一个递归遍历器负责规范化、排序、折叠以及大纲/网格 ViewModel；UI 默认最多展示 Module → Tool Group → Category 三层 Group，更深配置必须被校验拒绝或显式降级，避免形成不可理解的深树。
- 纯模型单元测试至少拒绝循环、重复 ID、跨 Module 冲突、父节点可执行、叶子带子项、未注册 `toolId`、非法默认叶子和重复 Action ID；树形与图标渲染必须从同一规范化结果产生。

```ts
type KtcNavigationNodeKind = "group" | "tool";
type KtcNavigationGroupRole = "module" | "tool-group" | "category";
type KtcNavigationNode =
  | {
      readonly kind: "group";
      readonly role: KtcNavigationGroupRole;
      readonly id: string;
      readonly children: readonly KtcNavigationNode[];
      readonly defaultToolId?: string;
    }
  | {
      readonly kind: "tool";
      readonly toolId: string;
    };
```

导航 JSON Schema 只约束 `kind`、Group `role`、层级与 Tool 引用；Surface 和实例策略由 TypeScript Tool Registry 的 `KtcToolDescriptor` 约束，避免 Manifest 与实现各存一份后漂移。短名称显隐是用户级显示偏好，不写回能力树。校验器还要限制 Module 只出现在顶层、Tool Group 属于 Module、Category 不跨模块，并为未来新增枚举值要求 schemaVersion 升级或兼容分支。

0.9.0 的首个原型已具备直接叶子激活、MRU、统一 Primary 投影、可选 companion 和 session/revision 动作校验；尚未完全泛化的部分包括 companion 工具 ID 的显式集合、Code Assistant/CAD 的兼容分支及若干工具专用 Webview 消息。下一阶段应先收敛这些模型边界，不把大纲/网格切换变成第三套运行时。

迁移完成后，`codeAssistant` 只是一条普通 Tool Group 配置；通用 Tool Navigator 自动渲染其分类和叶子，删除专用 Tree DOM、feature 状态和消息分支。Code、CAD 与后续模块使用同一 schema，只提供各自配置与 Tool 实现。

现行交互是：点击 Ribbon Group 选择并显示 Tool Navigator；Current Tool Header 的折叠动作独立控制 Body，Header 的 `×` 独立关闭逻辑工具并按 MRU 回退。早期“重复点击当前 Group 折叠 Surface、删除整行 Header、使用右上悬浮 `×`”方案已废弃，不得继续保留第二套状态机。`AGENTS.md`、权威前端规则、实现与测试必须在同一节点保持一致。

### 13.0 2026-09-06 历史大纲 / 网格原型（已替代）

本节只保留早期实验记录；现行候选由 13.0.1 和 [Phoenix Webview Preview 原型跟踪](Phoenix-Webview-Preview原型跟踪.md) 替代。不得用本节的大纲 / 网格、旧外壳或 Open Items 结论指导新实现。

本地 `v0.9.0` 已先做一条可回退的纵向切片，用“代码辅助”验证框架控件，而不是继续扩写专用 DOM：

```text
┌─ 功能目录（7） ───────────────────── [大纲] [网格] ┐
│ 大纲：                                                    │
│   ▾ C++ 整理（4）                                         │
│      编译工具                                             │
│      头文件引用修正                                       │
│                                                          │
│ 网格：                                                    │
│   ▾ C++ 整理（4）                                         │
│   [编译工具        ] [头文件引用修正] [成员排序 ...]       │
└──────────────────────────────────────────────────────────┘
```

- `大纲 / 网格` 采用可点击文字，避免只有图标时含义不清；两项使用 `aria-pressed` 表达当前状态。
- 大纲始终单列。网格使用 Tool Navigator 自身宽度而非窗口宽度排列：极窄一列，空间增加后自动形成多列；用户选择的模式不会因为拖动侧栏而自动跳变。
- `group | tool` 纯 TypeScript 模型、校验器、Web Component 与代码辅助清单已经分离。控件只接收可序列化 ViewModel 并发出 `activate / setMode / setExpanded / setGroupExpanded` 语义事件，不依赖 VS Code API、存储或业务 Controller。
- 代码辅助仍通过兼容适配器把 `packageIncludes / autoBuild` 交给既有 Editor 路由，其他叶子交给统一 `selectTool`；因此本切片验证的是数据与 View 分离，不宣称完整 Tool Registry / Action Map 迁移完成。
- 布局与分组展开仍复用原用户级状态；切换布局不触发工具，不改变 MRU、Current Tool Block、任务或 Editor。
- 正式外壳不包含底部“打开项栏”；内部热预览可将其作为 Current Tool Block 内的可开关实验，必须与能力导航分开评审。

原型自动证据：纯模型、Web Component、Primary 接线和 Host 持久化聚焦测试通过；正式构建已生成独立 `dist/ktc-tool-navigator.js`。浏览器夹具加载真实 bundle 后已确认：极窄宽度回退一列、中等宽度两列、宽栏四列，均无横向溢出；点击叶子只更新选中态。真实 Development Host 已启动，但首次检查时机器锁屏，因此 VS Code 深/浅/高对比和真实侧栏观感仍待用户解锁后点检，不得写成已通过。

浏览器级快速点检可使用 [`tests/webview/tool-navigator.html`](../tests/webview/tool-navigator.html)；它只加载真实构建后的控件 bundle，已由 `.vscodeignore` 排除，不进入发布包。

### 13.0.1 Primary + Editor 内部热预览

为避免每次视觉调整都启动 Extension Development Host，仓库提供独立预览命令：

```bash
pnpm ui:dev
```

命令默认监听 `127.0.0.1:4173`，也可用 `pnpm ui:dev -- --port 5173` 指定本机端口。它使用仓库已有 esbuild 在内存中构建 `ui-preview/src/main.ts`，监听 TypeScript、HTML 与 CSS，并通过本地 SSE 在构建成功后自动刷新。服务只允许访问首页、样式、刷新客户端和内存 bundle；`package.json`、源码目录和其他仓库文件不会被当成静态目录公开。默认不生成 inline sourcemap，并校验请求 Host，避免把“仅绑定 loopback”误当成完整的资源隔离。

预览服务器、资源白名单、内存构建、SSE 和关闭生命周期已经抽到 [`scripts/webview-preview`](../scripts/webview-preview/README.md)；`scripts/preview-primary-ui.ts` 只保留 Auto Code 的路径与路由配置。当前先在本仓库验证，不发布、不迁 Wing。第二个消费者验证无需复制后，才考虑迁为 Wing 的独立开发期 package。

预览控制条不再提供“联合 / Primary / Right View”三种互斥宿主模式。Right View 始终作为工作台主区域存在，只保留一个 `Primary` 显示 / 隐藏开关；这个开关只用于检查布局与恢复，不代表 Tool 的 Surface 注册。

预览与正式 Primary 现已复用 `KtcPrimaryShell`、`KtcDirectoryBar`、`KtcToolbarStrip`、`KtcToolNavigator`、`KtcCurrentToolRegion` 和 `KtcOpenItemsBar` 等 Host-neutral Web Components；Preview 不调用 VS Code API、文件系统、进程或真实 Tool Action。已确认并正式接入的外壳为 `Directory → Toolbar（含下级导航）→ Current Tool → Open Items` 四段，Header 顺序为 `目录 → Ignore → Settings`；Current Tool 固定展开，Header 只保留独立 `×`，Body 是主要滚动区。真实 Host 整体视觉已经用户确认，治理规则与回归已按该方向同步；Open Items / MRU 重启持久化和焦点 / 溢出细节仍须继续点检。

Preview 继续作为“先原型、确认后同步正式实现”的长期沙箱。它在 Primary 与 Right 外层模拟 VS Code 宿主圆角和 gap，但这只是 preview chrome，不是插件能力；原生 View Header 的目录命令不支持 persistent 蓝色 toggle 高亮，因此 Preview 也不模拟该高亮。展开 Ribbon 按内容宽度布局、最小 `50px`，搜索替换入口使用正式短名称“替换”。

Toolbar 的唯一 `…` 在原型中必须打开溢出菜单，并让当前宽度下未显示的 Ribbon 工具全部可达；菜单选择与 Ribbon 选择进入同一激活通道。它不能只是视觉占位，也不能因展开/紧凑切换生成第二份 Ribbon DOM。

Open Items 已在原型中确认为独立第四段并接入正式 Primary，使用共享 `KtcOpenItemsBar`；它不再有显隐实验开关，也不嵌入 Current Tool Footer。用户接受其底部少量空隙属于 VS Code-only 宿主差异，不要求插件 CSS 强行抵消；后续仍需补齐 Open Items / MRU 的 Extension Host 重启持久化、完整多实例和焦点 / 溢出细粒证据。

工具宿主也不再采用互斥 `primary | editor` 身份。原型 Tool Catalog 演示 Primary 完整工具，以及 Right View + Primary companion 工具；同一个逻辑 Tool 最终由必需 `primary`、可选 `right` Surface、`instancePolicy` 和共享 `toolId + instanceId + sessionId` 注册。内容复杂度与 Right 是否存在由 Tool 消费者决定；详情、阶段与逐项验收以 [Phoenix Webview Preview 原型跟踪](Phoenix-Webview-Preview原型跟踪.md) 为单一真源。

内部热预览人工点检：

- [ ] Primary 显示 / 隐藏后，Right View 始终存在；隐藏区不占布局、不留下不可见焦点，恢复后状态仍可解释。
- [ ] Primary 四段顺序固定；Header 为目录 → Ignore → Settings，后二者只更新 Current Tool Block，Right View 标签数量不增加。
- [ ] Toolbar 展开与紧凑复用同一 Ribbon；唯一 `…` 可打开菜单，紧凑态中不可见工具都可从菜单激活。
- [ ] 固定目录始终一行，左侧无装饰文件夹，右侧只有一个实际选择按钮。
- [x] Current Tool 固定展开且无折叠入口；Header 的 `×` 可键盘触达，关闭按 MRU 回退，不关闭 Right View 或模拟任务。
- [ ] Body 是 Primary 唯一日常纵向滚动边界；Toolbar、目录、Header 与实验 Footer 不随 Body 滚动。
- [ ] Open Items 固定为独立第四段；短名称、横向溢出、栏内 `…`、MRU 和关闭语义均可用。
- [ ] 深色、浅色、高对比及窄/标准/宽三档下无裁切；tooltip 不被左侧边界遮挡，图标与文字不成为唯一状态信号。

`ui-preview/**` 与预览启动脚本都不进入 VSIX。预览通过只表示浏览器中的布局与交互方向可用，不能替代真实 VS Code 主题、Webview 生命周期、命令路由或 Windows CAA 点检。

### 13.1 分阶段开发与验证

后续按下面阶段推进。每个阶段必须独立形成可回退的本地节点；上一阶段的退出条件未满足时，不进入下一阶段，也不借机搬动领域算法。

| 阶段 | 开发内容 | 自动验证 | 人工验证与退出条件 |
| --- | --- | --- | --- |
| 0. 冻结 0.9.0 首个原型 | 归档旧两区 Shell、Toolbar 两态、MRU 和两个 Editor companion；Ignore 修正继续留在独立 fix 分支 | 全量测试、类型检查、文档门禁、构建、Wing 来源门禁、隐私扫描 | 已形成 `f847223`；只保留统一激活、MRU、companion 等可复用证据，旧外壳结论已废弃 |
| 0.1 历史无 Header 原型（已废弃） | 曾删除 Current Tool Header、用重复 Ribbon 点击折叠并使用悬浮 `×` | 历史测试结果仅作迁移回归输入 | 不得作为现行实现或验收目标，不继续维护第二套状态机 |
| 0.2 三段 Shell 迁移 | Header 收敛为 Ignore → Settings；DOM 固定为 Toolbar → 目录 → Current Tool；恢复紧凑 Current Tool Header，并分离折叠与 `×` | DOM 数量/顺序、ARIA、唯一滚动区、Header 路由、关闭/MRU/companion 回归；随后跑完整门禁 | 深/浅/高对比、280/360/560/760px、重载及真实 Extension Host 点检通过；旧目录显隐与无 Header 路径清零 |
| 0.3 Phoenix Webview Preview（已归档并同步外壳） | 冻结 `Directory → Toolbar → Current Tool → Open Items` 四段与 `目录 → Ignore → Settings` Header；Current Tool 固定展开；Right 常驻，只切 Primary 显隐；共享 Web Components 已同步正式 Host，AutoBuild、项目改名与 PackageIncludes 使用共享 Right Shell | 预览服务器、Tool Catalog、Store、四段顺序、legacy/compact 兼容、Primary/Right 分流及组件定向测试 | 浏览器注释已收敛且真实 Host 整体视觉已确认；Preview 长期保留。重启序列化、多实例、完整 Registry Builder 和焦点 / 溢出细粒点检继续按专用跟踪文档推进 |
| 1. 建立纯模型 | 新增 JSON Schema、`group \| tool` 可判别联合、Group role、Tool surfaces / instancePolicy、规范化器和诊断；为 Code TS catalog、CAD/module manifest 与现有 Registry 加只读兼容适配器，UI 仍用旧 DOM | Schema 正反例、循环/深度/跨来源重复 ID/defaultToolId/缺失实现/非法 Surface 组合测试；同一输入规范化结果稳定 | 现有 UI 像素与行为不变；所有来源合并后的旧工具清单和新规范化清单逐项相等，冲突来源 fail closed 并可诊断 |
| 2. 分离 Action 注册 | 先把 Auto Build 的消息协议收成单一 runtime decoder 与单一 View 发送端；随后让每个 Tool 注册静态 Action Map，Host 建立 `(toolId, actionId)` 索引，Controller 提供运行态和 handler，Shell Action 使用独立命名空间 | Auto Build schema/未知消息、重复 Action、未知 Tool/Action、disabled、旧 session/revision、跨工具串线和危险动作门禁测试 | Primary、命令与 Editor 触发同一 Action 时进入同一 Controller；未授权或过期消息统一拒绝；不再有 inline legacy sender 与 bundle sender 双协议 |
| 3. 单组导航原型 | 只选“代码辅助”一个多 Tool Group，从规范化模型渲染无标题紧凑 Tool Navigator；`Aa` 只切短名称，正式 legacy 呈现由显式兼容分支隔离 | 有名/纯图标两态叶子集合等价；键盘导航、焦点恢复、MRU、Group 披露和直接叶子激活测试 | 280/360/560/760px，深/浅/高对比点检；切名称不重新执行工具、不丢草稿和滚动位置 |
| 4. 简单叶子迁移 | 先迁无 Editor、无长任务的工具，例如头文件 ASCII 修正、编码修正、UUID；删除对应专用菜单分支 | 每个 Tool 的激活、Action Map、关闭/MRU、错误态与兼容命令测试 | Ribbon 选择 Group；Navigator、`…`、命令进入同一叶子；配置 `defaultToolId` 时另验 Ribbon 的显式续接。写盘类动作仍显示预览/确认并保留原门禁 |
| 5. 复杂双 Surface 验证 | 将项目改名和自动编译接入通用 Tool 注册；Primary 仅投影摘要与安全动作，Right 保留完整表单、列表和日志；两侧共享实例引用 | 双 Right 活动归属、attach/detach/destroy、draft/revision 握手、取消/停止、迟到消息与敏感文本裁剪测试 | macOS 可打开、配置和检查；项目改名 Diff/取消、编译 Output/停止可达；Windows 再做真实 CAA 启停 |
| 6. 跨模块验证 | 用同一 schema 合并 Code、CAD 与后续模块清单；模块包只贡献 Group/Tool 描述和 Tool 实现 | 跨 Module 重复 ID、缺包/缺实现、不可见模块、排序/固定、升级迁移和 Registry 来源测试 | Code/CAD 切换不生成第二套 Primary；缺失 CAD 时安全禁用并有明确诊断；本地 Wing 来源可证明 |
| 7. 外壳交互收口 | 按 Directory → Current Tool → Toolbar → 薄 Primary Shell 的风险顺序接入共享组件；Navigator 是 Toolbar 的 child slot，Open Items 复用现有组件 | 覆盖四段顺序、折叠、关闭/MRU、焦点、ARIA、任务存活、Right 生命周期及 Group/Tool 映射 | 迁入前集中更新正式契约与状态迁移；不恢复旧无 Header/悬浮关闭状态机，不形成聚合业务状态的 mega component |
| 8. 分批收口 | 逐批删除 Code Assistant/CAD 特判、专用 Tree DOM、重复 feature 状态和专用消息；保留兼容迁移窗口 | 全量回归、旧状态迁移、扩展 Host smoke、产物校验和差异清单归零 | 每批行为等价、可单独回退；最后完成浅/深/高对比、窄宽、重载、双 Editor 和 Windows CAA 发布矩阵 |

### 13.2 每阶段共同门禁

1. **模型门禁**：导航 Tree 只允许 `group` 或 `tool`；Group 不可执行，Tool 不可带 `children`，Action 不进入 Tree。
2. **路由门禁**：Group 入口只提交稳定 `groupId`，Tool 入口只提交稳定 `toolId`；只有显式 `defaultToolId` 可把选组继续解析成叶子激活。所有业务动作最终只提交 `(toolId, actionId)`，不得由按钮文字、首个子项或 DOM 位置推断处理器。
3. **状态门禁**：MRU、选中、Navigator 名称显隐、Right lifecycle、instance/session/revision 分层保存；布局切换不得复制任务状态。
4. **安全门禁**：文件写入、改名、进程和删除仍由 Tool Controller 做领域校验；Primary 快照只允许短摘要，不携带源码、完整命令、凭据或绝对路径诊断。
5. **体验门禁**：主题 token、键盘、焦点、ARIA、窄宽与唯一滚动边界必须随阶段测试；Header 动作保持最小，不把 Tool Action 上移到原生 Header。
6. **兼容门禁**：新增 schemaVersion、枚举值或状态结构必须有旧版本迁移/拒绝策略；在旧实现移除前，以规范化清单和行为测试证明等价。
7. **归档门禁**：每阶段提交前复核分支、版本、起点、未归档文件、Wing 来源、隐私和文档清单；只提交本阶段范围，不自动合并或推送。

### 13.3 明确不在当前模型阶段处理

- 不重新设计已确认的原生 Header、固定目录或 Toolbar 展开/紧凑视觉。
- 不把 Ignore fix 分支、项目改名第二阶段正则/残留探测、编译脚本功能扩展混进导航模型提交。
- 不因 Tree 数据化而把脚本、handler 或任意可执行代码写入 JSON。
- 不让每个工具自行复制 Current Tool Header；Header 由 Shell 统一渲染，折叠与 `×` 进入同一外壳但保持独立动作。旧“无 Header/悬浮关闭/重复 Ribbon 点击折叠”模型不得恢复。
- 不实现大纲和网格两套状态机；二者必须由同一规范化模型与 ViewModel 派生。

设计依据：VS Code 建议限制 View 与 Header actions 数量、优先使用内置图标、避免重复功能，并要求 Webview 可主题化且具备 ARIA 与键盘导航；官方还明确不建议把 Tree item 当成单击即执行的按钮。所以下一原型应让 Navigator 负责选择 Tool、Action 由 Current Tool Block/命令入口触发，并把布局切换保持为一个次要动作，不为大纲/网格复制两套运行时。原生 `view/title` 仅承载 Ignore、Settings 两个固定动作，其余全局定制进入 Toolbar 唯一 `…`，工具动作留在 Current Tool Body。参考 [VS Code Views UX Guidelines](https://code.visualstudio.com/api/ux-guidelines/views)、[Sidebars UX Guidelines](https://code.visualstudio.com/api/ux-guidelines/sidebars)、[Webviews UX Guidelines](https://code.visualstudio.com/api/ux-guidelines/webviews) 与 [Contribution Points](https://code.visualstudio.com/api/references/contribution-points#contributesmenus)。

## 14. MVC + Tool Registry 目标架构

### 14.1 参考模式与本仓取舍

VS Code 自身把可发现的 Command contribution 与运行时 `registerCommand` handler 分开，并保证同一 command ID 可被菜单、快捷键或代码调用；Tree View 则把数据提供者与 TreeItem 呈现分开；Webview View Provider 主要承担生命周期和消息桥接。0.9.0 不照搬原生 TreeView，也不引入重型框架，而复用这些成熟边界：**描述与执行分离、数据与呈现分离、Host 与 Webview 分离、可见性与可执行性分离**。

当前兼容实现可以工作，但职责仍集中：`tools/types.ts` 同时承载大量全局消息、UI 状态和 Tool 接口；`sidebarViewProvider.ts` 同时处理 Shell、领域分支和消息路由；`panelHtml.ts` 同时生成 Shell、各工具 DOM 与事件接线；`tools/registry.ts` 只是进程级可变数组。迁移目标不是一次拆完这些文件，而是先建立可测试的边界，再让旧路径逐个经过适配器退出。

### 14.2 分层职责

| 层 | 只负责 | 不负责 |
| --- | --- | --- |
| Navigation Manifest / Schema | Group/Tool 层级、文案、图标、顺序、默认叶子、允许的 Navigator 模式 | handler、脚本、VS Code API、运行状态、Primary/Editor presentation |
| Tool Registry | 在 Extension activation 的 composition root 一次性装配 Tool descriptor、Action Map、Controller factory、ViewModel projector、可选 companion；校验后冻结索引 | 导航状态、业务任务、DOM、全局可变单例 |
| Model | 纯 TypeScript 的导航图、Tool/session 状态机、Action token、领域 Analyze/Plan 数据 | `vscode`、DOM、文件写入、通知 |
| Tool Controller | 解析已验证的 Tool intent，拥有任务/session/revision，执行领域门禁并调用 Port | 拼 HTML、根据按钮文字分支、直接操纵另一个 Tool 状态 |
| ViewModel Projector | 从 Model snapshot 派生最小、可序列化、可脱敏的 Primary 或 Editor 投影 | 保存权威状态、执行命令、读取文件系统 |
| View / Web Component | 使用主题 token 渲染 ViewModel，维护短暂焦点/滚动状态，发出结构化 intent | 业务算法、路径推导、Action enablement 真源、跨工具路由 |
| Host Router | 校验 Shell/Tool envelope、查 Registry、验证 payload/session/revision/enabled/risk，再调用所属 Controller | 维护每个领域的特判 switch、猜测默认 Tool、信任 Webview payload |
| Ports / Adapters | 封装 VS Code 命令、配置、存储、文件系统、进程、SCM/Output 与平台差异 | 反向依赖 View 或把机器路径写入项目配置 |

静态代码依赖与运行时数据流必须分开描述，避免为方便发消息而让 View import Router 或 Controller。静态依赖固定为：

```text
View → serializable ViewModel/contracts
Host Provider → Host Router → immutable Registry interfaces
Tool Registration → Controller interfaces + ViewModel projector
Controller → pure Model + Port interfaces
VS Code/FS/Process adapters → Port interfaces
Composition root → Manifest parser + Registry builder + Host adapters
```

运行时数据流为：

```text
Manifest → normalized catalog → Navigator ViewModel → View
View semantic intent --postMessage--> Host Router → Registry Action → Controller
Controller → Model snapshot → ViewModel projector --postMessage--> View
```

这里的 `postMessage` 箭头是端口事件流，不是静态 import。下层不得反向 import 上层。纯 Model/Normalizer/Action routing 不导入 `vscode`；View 不导入 Tool Controller 或 Host Router 实现；Tool 之间协作只能经显式共享 Port 或领域服务，不能从全局 Registry 抓取彼此实例。

### 14.3 Registry 与 Action Router 草案

Registry 使用“实例化后冻结”的 composition-root 模式，不在模块 import 时向全局数组产生副作用。Extension 每次 activation 创建一个新实例并随 `ExtensionContext` dispose；测试可直接构造隔离 Registry，不再依赖清空进程级单例。构建索引时同时检查 `toolId`、Group ID、Module provider ID 和每个 Tool 内的 `actionId`；重复项在开发/测试直接失败，在正式 Host 禁用冲突贡献并输出双方 source owner，不允许后注册者静默覆盖前者。

```ts
interface KtcToolRegistration<TSnapshot> {
  readonly descriptor: KtcToolDescriptor;
  readonly actions: Readonly<Record<string, KtcToolActionDefinition>>;
  readonly createController: (ports: KtcToolPorts) => KtcToolController<TSnapshot>;
  readonly toPrimaryViewModel: (snapshot: TSnapshot) => KtcPrimaryToolViewModel;
  readonly editorCompanion?: KtcEditorCompanionRegistration<TSnapshot>;
}

interface KtcToolIntentEnvelope {
  readonly toolId: string;
  readonly actionId: string;
  readonly requestId: string;
  readonly sessionId?: string;
  readonly revision?: number;
  readonly payload?: unknown;
}
```

每个 Action Definition 同时提供 payload 的运行时 decoder/validator；TypeScript 类型不能替代 Webview 边界校验。Router 固定执行：解析 envelope → 查找 Tool/Action → 校验 placement、enabled、workspace trust、session/revision 与 payload → 记录脱敏审计 → 调用 Controller。Controller 返回状态转移或领域结果，Router 不解释结果正文，只触发对应 ViewModel 重新投影。

VS Code 原生命令只做薄适配：命令 ID 保持兼容，handler 把参数转换成同一个 Tool intent 后交给 Router。`package.json` 的 `when` 控制入口是否出现，`enablement`/运行态共同控制能否执行，但 Controller 仍必须做最终校验，因为隐藏或禁用 UI 不是安全边界。

### 14.4 建议目录与迁移映射

```text
src/core/toolRuntime/              # Host-neutral，可由 Auto/CAD/Wing 复用
  navigationContracts.ts
  navigationSchema.ts
  normalizeNavigation.ts
  actionContracts.ts
  actionRouterModel.ts
  toolSessionModel.ts

src/tools/
  registry.ts                      # composition root / immutable index
  legacyToolAdapter.ts             # 迁移期唯一兼容层
  <tool>/
    registration.ts                # descriptor + action map + factories
    controller.ts                  # 领域流程与状态机
    primaryViewModel.ts            # 最小 Primary 投影
    editor/                        # 仅复杂工具需要

src/sidebar/
  shellController.ts               # 目录、Toolbar、当前 Tool、MRU
  toolHostRouter.ts                # Shell/Tool envelope 路由
  panelHtml.ts                     # 迁移期 View；逐步只消费 ViewModel
```

实际迁移按现有文件小步进行，不先做目录大搬家：

1. 先在 `core/toolRuntime` 增加纯契约、Normalizer、Registry builder 和测试；现有 `KtTool` 通过 `legacyToolAdapter` 注册，UI 不变。Normalizer 必须把 Code 的 TS catalog、CAD/module manifest 和未来包贡献合成一个带 source owner 的 catalog；开发/测试遇到跨来源重复 Tool/Group/Module ID 直接失败，正式 Host 则禁用冲突贡献并输出来源诊断，不能由 `sendInit` 拼接或 `registerModuleBlockProvider` 后注册静默覆盖。
2. 在通用 Router 之前先收敛 Auto Build 协议：新增 `autoBuildMessages.ts` 的运行时 parser/decoder，让 bundle `autoBuildViewEntry.ts` 成为唯一发送端，删除 Controller HTML 中仍产生 schemaVersion 1 的 legacy inline handler。未完成该步时，Auto Build 不作为通用 Router 的示范 Tool。
3. 把 `WebviewInboundMessage` 分成小型 Shell envelope、通用 Tool intent 和迁移期 legacy union；新 Tool 禁止再向全局 union 增加专用顶层 message type。
4. 从 `sidebarViewProvider` 抽出 Host Router。Provider 只保留 Webview 生命周期、Shell context 与 postMessage；旧领域分支每迁一个 Tool 就删除一组。
5. 先让一个无写盘简单 Tool 完整使用 registration/controller/projector，再迁头文件 ASCII、编码和 UUID；每次只迁一个领域并保留旧命令 ID。
6. 保留现有 Module Shell v2 适配器：CAD provider 的 Host 生成 HTML 与无 `toolId` 的 `moduleBlockAction` 继续按 module owner 验证并路由，不冒充新 Tool Action。只有 CAD 发布新的 versioned Tool contribution 契约并通过兼容测试后，才删除 v2 adapter；不能直接以新 Registry 覆盖旧 CAD 接口，也不能在 0.9.0 单方改变 `ktAutoCodeModule` Manifest。
7. 最后迁 Project Rename/Auto Build hybrid Tool，把现有 companion state 作为通用 session/revision 模型的 fixture，而不是重写已经验证的安全门禁。
8. 等 Auto Code 与 CAD 至少各有一个消费者、API 命名稳定且无 Host 类型泄漏后，再把 Navigator/Current Tool Body/Action Bar 的 Host-neutral 部分迁到 Wing；Wing 组件只接收 props 并发事件，不拥有业务 Controller。

### 14.5 测试金字塔与停止阀

- **纯模型层**：Schema、Normalizer、Registry、Action Router、session/revision、MRU 和 ViewModel 使用快速单元测试，覆盖非法输入与不变量。
- **Controller 层**：通过内存 Ports 验证文件/进程/配置调用顺序、取消、幂等、漂移与权限；不启动 VS Code，也不写真实用户目录。
- **View 契约层**：验证同一 ViewModel 在 outline/grid、深/浅/高对比、窄宽下的 DOM、ARIA、焦点与事件 envelope；不复制 Controller 测试。
- **Host 集成层**：只验证注册、命令兼容、View 生命周期、跨 Editor 激活、context key、存储 scope 与 Port 接线。
- **人工层**：每阶段只点检新增交互；Windows CAA/PowerShell、SCM Diff、真实主题与极窄宽度保留为平台矩阵。

旧目录显隐 key 仅作为一次性兼容迁移输入，不得继续驱动现行 UI；Ribbon 布局、Module 状态、工作目录、Ignore 开关，以及 Project Rename/Auto Build 已存在的 session/配置 key 仍是迁移兼容输入。新运行时只能兼容读取并通过显式 schema 升级写回；不得静默改名、清空或把机器级路径迁入 Workspace Folder 设置。

每阶段设置停止阀：兼容清单不相等、出现双状态真源、Router 需要新增 Tool 特判、View 必须读取文件系统、Controller 依赖 DOM、旧 session 可执行 Action、或无法独立回退时，立即停止扩迁并修复当前阶段；不能用“后续统一清理”带病进入下一批。

参考：[VS Code Commands](https://code.visualstudio.com/api/extension-guides/command)、[Tree View API](https://code.visualstudio.com/api/extension-guides/tree-view)、[Webview View sample](https://github.com/microsoft/vscode-extension-samples/tree/main/webview-view-sample)、[When Clause Contexts](https://code.visualstudio.com/api/references/when-clause-contexts) 与 [VS Code Extension Samples](https://github.com/microsoft/vscode-extension-samples)。

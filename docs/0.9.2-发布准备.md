# Auto Code 0.9.2 Registry 发布准备

状态：current；Registry 源码准备，正式制品结果以发布工作树机器回执为准；未上传市场

Owner：KT Auto Code maintainers

适用版本：0.9.2

最后核验：2026-09-10

## 归档与本轮范围

- 本地联合功能已压缩为 `0497b3aeae9f54a46f50d5dbc0dc6a85a00f3a6b`，父节点 `2e3849f`；主库 `user` 的对应 cherry-pick 为 `e2896e789f27e780e2af37d152d46c8a3c85610a`。两者 tree 同为 `491e0d22ffaf7abad2daa4df9438c9168efceb9c`。原三个节点和未提交状态保留本地备份引用。
- 本轮只升级已发布的精确 Wing 依赖、更新依赖门禁与必要回归、补齐文档和打包证据，另留提交；不重写以上归档，不开展新功能。
- 用户要求等待 Wing 发布再打包；现在该前置条件已解除。AI 仅制作本地 Registry VSIX，不执行 npm publish、Git push、标签或 Marketplace 上传。
- 0.9.0 不正式发布。旧 0.9.1/0.9.2 本地 Wing VSIX、源码快照和哈希保留历史，不覆盖、不作为本轮发布输入。

## Wing 发布来源与精确依赖

Wing 发布源为 `develop/v0.7.4@d2f73a48409c195a500787c16510c7d05aab1ea0`，tree `9d2c829d51e5f7d6c8687e8cff03aabbefc0988f`。2026-09-10 已独立读取 npm Registry metadata，以下 shasum 与发布回执一致；lockfile 使用 Registry 返回的 SHA-512 integrity，不使用本地路径或 override。

| 发布包 | 版本 | Registry tarball shasum |
| --- | --- | --- |
| `phoenix-wing`（Auto 不直接依赖根包） | `0.7.4` | `9e3c533f06a916427d754663e90c633f02d63553` |
| `@phoenix-wing/code-core` | `0.6.6` | `9f7d221574545cb2bc959d9092dd07f13e41ac52` |
| `@phoenix-wing/kt-codegen` | `0.6.6` | `320fc71f034a6c876f7b32b1c5c8c38ccce3ef6a` |
| `@phoenix-wing/git-core` | `0.6.4` | `290763729fd359f3049433702b42fd6b181ca40a` |
| `@phoenix-wing/git-node` | `0.6.5` | `2769d2b6fc73011a5323a4c45916f52a694c71e6` |
| `@phoenix-wing/run-core` | `0.6.4` | `e8de2e9edb015a03dbfd7c13b4bda96cabcc7998` |
| `@phoenix-wing/run-node` | `0.6.4` | `ac6a883d817eda9fcac5b7f194bf0ab0d980a55f` |

六个实际安装包均解析到 consumer 的 `node_modules/.pnpm` Registry 安装目录。`kt-codegen` 是 ESM 公共入口；真实导出 `KT_CODEGEN_GENERATOR_VERSION` 为 **1.0.2**，与 Auto 预检缓存一致，不能用 CommonJS `require.resolve` 检查它的 ESM-only exports。

## 源码准备验证与制品门禁

- Registry prepare 和类型检查已通过；清除 `PHOENIX_WING_ROOT` / `PHOENIX_WING_DEV_MODE`，正式构建不读取并列 Wing，不把开发 symlink 写入依赖或制品。
- 源码提交前最终 Registry 全量回归 **257 文件 / 1924 测试全部通过，无跳过**，包含启用本机 CMake 的临时编译夹具；类型检查、**120 份文档**与架构 **266 source / 27 pure graphs / 25 view roots**通过。发布工作树仍须从 frozen lockfile 完整重跑，不复用本轮开发目录构建产物。
- 原 Git 提交图分页测试不再仅限本地 Wing：在精确 Registry 下核验时间顺序、merge/tag、remote 引用、不透明 cursor 和 HEAD 漂移。新增已安装 Wing / Auto 规则版本一致性断言；完整测试结果以最终运行日志登记，不保留跳过以规避新版依赖验收。
- macOS VS Code **1.137.0** 真实 Extension Host 在自建临时目录完成 **16 个代表流程**：打开、预检、冲突、应用、保存/重载、回滚、缓存持久化与失效、JSON 重建/标签隔离、头文件服务、Git/Run、项目改名及取消。未使用用户工程执行写入或清理；不等同于新清理 Right 的完整 UI 人工验收。
- 源码提交后同步主库 `user`。正式包仅从共享 `worktrees/kt-auto-code-release` 中选定完整提交的 **clean detached HEAD** 产生：frozen lockfile 安装、`release:preflight`、Registry prepare、`KTC_TEST_NATIVE_CMAKE=1 release:check`、最小 Host 回归、`release:receipt`。
- 发布入口会校验包内实际生成规则 **1.0.2**、构造函数边界、Combo 注释、清理运行时、全部 Wing 内嵌和必要资源；检查无 Preview、源码 map、node_modules、机器路径或本地模式。最终 `.vsix.sha256`、`.vsix.build-provenance.json`、`.release-receipt.json` 与 VSIX 一同本地留存；未取得回执前不宣布正式制品门禁通过。
- 这次无新业务算法或界面修改，规则版本仍是 **1.0.2**，不会仅因依赖发布/打包再次升级。

## 三组手工点检与残余边界

1. **macOS 正式界面**：确认编译工具 Right Header 的“清理”及四个折叠 Block、固定顶边、原生 YAML 编辑/用户保存、当前工作目录探测与 Output 明细；在自建测试目录中核验规则清理和失败保护。弹窗仍属于 Right Webview，独立浮窗没有在本版实现。
2. **Git 与自动代码**：JSON 文件名标签、滚动/Primary 联动、分支切换与区间预检、remote 详情；生成时确认 `@codegen-rules-version 1.0.2`、构造函数 END 对齐、Combo 字段注释及旧缓存失效。原型的 BUILD_MANIFEST 导入、配置/脚本/任务闭环不冒充正式 provider 已完成。
3. **Windows 待测**：Combo 在浅色/深色/高对比多主题下的下拉可读性、真实 CAA 运行及 clang-format 版本/配置；`Invoke-AutoBuild.local.ps1` 首行错误仍待取得原文件与完整报错，尚未定位，不能宣称修复。屏幕阅读器专项按用户要求不做。

详细步骤见[清理与编译工具点检表](编译工具/后续原型计划.md#本轮手工点检清单)。打包通过仅说明制品与来源门禁通过，不把上述未验平台改写为全平台验收，也不表示已经公开发布。

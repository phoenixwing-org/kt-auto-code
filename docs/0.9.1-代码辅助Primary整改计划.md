# 0.9.1 代码辅助 Primary 整改计划

状态：本地内测收尾（五项 Primary 与 Git 正式修正完成；Git Right 原型已移至 0.9.2）

Owner：KT Auto Code maintainers

适用版本：0.9.1

最后核验：2026-09-10

## 开发起点

- 开发分支：`v0.9.1`，沿用 `worktrees/kt-auto-code-project-rename-analysis` 工作树。最初从本地 `v0.9.0@824edd0` 建立；2026-09-09 按用户要求将开发基线从 `93bc020ed` 迁至其已 cherry-pick 的 `f9fac1e`。两提交文件树完全相同，仅原子移动分支引用，未重写工作文件或暂存区；`824edd06b4` 及更早历史由用户处理，本轮未重复搬迁。
- `v0.9.0` 分支、内测 VSIX、来源回执和 SHA-256 保持归档，不因新版本开发重写；未推送或发布。
- 开发期间曾沿用 `0.9.0`；2026-09-10 按用户收尾指令将 `package.json` 调整为 `0.9.1`，仅准备本地内测包，不覆盖 0.9.0 归档制品。冻结、来源与未验边界见 [0.9.1 内测收尾](0.9.1-内测收尾.md)。
- 已核对五项原动作并接入 Preview。用户明确回复“可以同步正式”“我看了一遍，OK”后，2026-09-10 完成同一批组件的正式接入，沿用真实 Host 动作、配置和安全确认。
- 迁移前本地备份：`dist/archives/v0.9.1-before-f9fac1-20260909T132410Z/`，含历史 bundle、24 项未提交文件的补丁/完整内容及现存 VSIX，共 45 个文件完成解包哈希核验。旧基线另有 `codex/archive-v0.9.1-before-f9fac1-20260909T132410Z` 备份分支；迁移回执证明全部归档文件与暂存区不变。此后开发文档的迁移记录属于新的正常修改。

## 范围

用户已将代码辅助下**仅有 Primary 的工具**排入下一阶段；不混入 0.9.0 收尾。

候选：C++ 成员排序、头文件 ASCII 修正、编码修正、UUID 替换、CAA UI。实施前逐项核对现有入口与辅助页面；不因本次整理新增 Right，也不删除已有详情或外部工具能力。

头文件引用修正与 Git 的 0.9.0 基线已本地内测归档；人工未验项及正式 Registry 发布门禁仍见[发布冻结清单](0.9.0-发布冻结清单.md)，不混为本版新增功能。0.9.1 按用户后续反馈追加 Git 正式修正及自动代码滚动、Header 收尾，详见下节，不重做搜索替换、项目改名、Run 和编译工具页面。

## 改造要求

1. 以编译工具截图为样式参考：工具标题下面第一行放紧凑的内部文字工具栏。原有操作条或操作 Block 迁入这一位置，按各工具的实际动作命名，不照搬“打开/保存/另存”等无关按钮。
2. 补齐各工具的 UI Preview 效果，确认后同步正式实现；尽量共用组件，不维护两套 DOM 或业务逻辑。
3. 默认文字按钮；用户明确要求时才用纯图标或图标加文字。保留可用性、忙碌态、键盘操作及必要日志。
4. 只调整 Current Tool 内部，不改变外层 Shell、目录、导航、关闭或滚动规则。未要求删除的动作必须保留并记录新位置。

## 分工具 TODO

| 工具 | 原动作与新位置清单 | 内部工具栏 | UI Preview | 正式接入与验收 |
| --- | --- | --- | --- | --- |
| C++ 成员排序 | 扫描排序、应用所选 → 首行；结果选择/定位/差异/还原保留 | 已接共享文字条 | 用户确认；内存应用/取消已验证 | 已接真实 Host/Wing；投影及动作回归通过 |
| 头文件 ASCII 修正 | 预检、修复 → 首行；范围、GBK/BOM 选项与定位保留 | 已接共享文字条 | 用户确认；直接修复/取消/详情已验证 | 已接真实 Host；选项、问题定位和直接修复回归通过 |
| 编码修正 | 预检、转换、更多设置 → 首行；目标编码、范围与结果保留 | 已接共享文字条 | 用户确认；目标预检/转换/详情已验证 | 已接真实 Host；项目目标配置和转换回归通过 |
| UUID 替换 | 扫描 UUID、替换所选 → 首行；生成策略、选择/取消/差异保留 | 已接共享文字条 | 用户确认；内存映射替换已验证 | 已接真实 Host/Wing；策略失配及迟到状态保护通过 |
| CAA UI | 扫描 CATDlg、Desk Tools 设置、连接/重新检测 → 首行；源码/外部编辑器打开保留 | 已接共享文字条 | 用户确认；扫描/连接/模拟交接已验证 | 已接真实 Host；连接、扫描、打开/交接路由回归通过 |

入口核对：正式布局与动作投影在 `src/sidebar/panelHtml.ts`，工具声明在 `src/tools/{reorderMembers,headerAscii,encodingFix,uuidReplace,caaDialog}/index.ts`。Preview 通过 `previewCodeAssistantSurface.ts` 缓存五项实例，`main.ts` 已接入导航、目录变更及重置/销毁；未知工具继续原路由，不新增 Right。普通工具关闭或切换保留内部选项与结果，目录变更使旧计划失效。结果行操作不因首行工具栏改造被删掉或搬成无上下文的全局按钮。

实现边界：`KtcPrimaryActionBar`、`KtcTextRepairPrimary`、`KtcSelectionPrimary`、`KtcCaaPrimary` 仅承接展示/事件。正式 `codeAssistantPrimaryEntry.ts` 通过三个纯 adapter 投影 Host 状态；`panelHtml.ts` 负责原协议消息及本地 UI 状态，已移除旧重复 DOM/渲染/专用样式。排序和 UUID 的结果区仍为 Wing 组件，真实扫描、确认、写入和 Desk Tools 交接继续由原 Host 执行。内存文件、纯算法样例和模拟对话框只在 Preview adapter，构建门禁禁止其混入正式共享 bundle；无新增 Right 或外层 Shell 改动。

正式回归补充：每工具缓存独立实例，隐藏/关闭/忙碌时不发动作；保留范围、GBK/BOM、项目编码、详情及真实路径定位。UUID 本地策略与 Host 冻结计划分离，策略不一致时保留查看、禁用选择和写入；旧回执不能把用户策略改回或恢复错误写入资格。HTML 桥接测试执行真实页面脚本与共享组件，验证五工具模型、消息、切换和持久化，不以源码关键字测试代替交互验证。

验收逐工具记录：按钮不漏、窄栏不溢出、主题可读、禁用态正确、Preview 与正式行为一致。分支建立不代表上述页面已改造或验收。

本轮浏览器检查：ASCII 直接修复、取消及详情；编码选择 GBK 后预检与转换；排序应用两条后保留差异/还原入口，Esc 取消还原不改变结果；UUID 确认替换三个内存文件；CAA 扫描、连接及模拟交接。窄栏、浅色、深色和高对比已检查，用户已确认五项原型。正式接线自动验收与真实文件手工写入验收分开记录，后者不冒充已完成。

## 后续 TODO：清理对话框快捷操作

- [ ] 清理对话框内容首行增加紧凑的文字按钮栏，放置常用快捷清理入口；用户点击按钮即可发起对应清理。
- [ ] 包含“Phoenix 缓存清理”入口，对应 `.phoenix/cache/`（`.phoenix` 已隐藏，下层 `cache` 不再加点）。
- [ ] 清理范围、执行方式、预览/确认、运行锁及失败恢复后续单独研究。本轮仅记录需求，不接通清理动作，不删除任何缓存；“点击发起”不预先决定是否跳过安全确认。

路径复核：保留现有 `.phoenix/cache`，未迁移或删除缓存。PNXCaaStudy 顶层 `.cache` 初次检查只见 `clangd/index` 索引，现有 clangd 日志也显示该工程的后台索引任务。用户曾观察到函数排序后出现，但再次尝试未复现，暂记观察项；不能把排序认定为创建原因。排序扫描只读文件并调用纯文本算法，打开源码/差异及写入之后语言服务的间接反应仍需独立复现。该目录不纳入 Phoenix 缓存清理的默认范围。

- [ ] 优先核对排序后的语言服务联动：区分扫描、查看差异、打开文件和应用排序，关联缓存文件名/修改时间与 clangd 日志；用户观察缓存涉及排序文件，保留此线索，不因一次未复现排除间接关联。

## Git 修正与补充 TODO

- [x] 查清 `temp` 误选问题（2026-09-09）：旧图显示全部本地分支，`--topo-order` 把 `temp` 排在第 30 条；所选 8 个 OID 实属 `v0.9.0/v0.9.1`。未发现 worktree 串用或近期算法回归，安全校验正确，加载范围和提示造成误导。
- [x] 正式修正：合并 View 固定读取当前 `HEAD` 可达的提交及分叉；其他分支独有提交不显示，共同祖先与已合入分叉保留。不提供“查看全部分支”混合图。
- [x] 用户正式界面验收（2026-09-09）：明确确认三项 OK——Right 当前分支下拉框确认切换并清空旧勾选；只显示当前分支涉及的分叉并按时间排列；未提交改动或其他 worktree 占用时禁止切换。合并写入不据此视为已获人工验收。
- [x] 验收追加已实现并回归：分支切换被阻止/失败时增加明确的 VS Code 提示对话框，保留现有页面/输出原因；主动取消不误报失败，旧会话迟到错误不弹框。新增对话框待用户实看。
- [x] 长提交说明自动单行截断，右侧 hash、作者与时间独立布局，完整内容保留悬停提示；长作者和 refs 也可截断。维持原图轨道高度，不改变选择或 Git 算法；新构建外观待用户确认。
- [x] 2026-09-10：合并图增加 `remote` / `remote ×N` 徽章，显示完整远端引用及符号 HEAD 指向。只读当前仓库的本地 `refs/remotes`，按完整 OID 标记引用所在节点，不把本地分支/tag 当成远端，不表示实时服务器状态、不自动 fetch。用户指出的 Wing `7563cba` 本地确有三条远端跟踪引用。仅补 Auto 展示数据，不改 Wing 算法、提交图范围/排序或合并资格。
- [x] 提示交互按用户反馈收敛：`remote` 保留问号光标，点击才展开详情，再次点击、Esc 或点击外部关闭，不自动悬停弹出；分支、HEAD、tag 恢复普通标签，无提示或专属 hover 效果。长文本保留原选择能力、不用问号，停留 500ms 后显示完整内容；移出、点击、滚动、拖动或关闭页面会取消待显示提示。用主题化 DOM 浮层替代触发不稳定的原生 `title`，不发 Host 消息或改变勾选。
- [x] 按提交时间交错排列，同时保留父子拓扑；同标题不同 OID 独立显示。游标 v2 防止旧排序的分页状态混入。
- [x] 非当前分支主线的提交置灰并禁选，Host 同样拦截并提示不能合并。主线中间连续区间仍可选，不要求从 `HEAD` 开始；历史改写安全校验保留。
- [x] Right 绑定工作树、分支与 `HEAD`；同 OID 换分支也使旧上下文失效，提示重新打开并停用旧选择。重载首次读取前冻结会话归属，迟到成功或失败均不能重开已关闭视图或覆盖新视图。
- [x] Right“当前分支”combo：确认后切换该工作树的实际分支，成功重载并清空旧选择；取消或未切换的失败保留原草稿。脏工作区、其他 worktree 占用、身份变化或确认期间关窗会阻止派发；不 stash、不 force、不创建分支。
- 已移出本轮：用户于 2026-09-10 明确将 Git“合并区间”Right 原型补齐放到 [0.9.2](0.9.2-Git合并区间原型计划.md)，不再阻塞 0.9.1 内测收尾；原型未完成，不勾作已实现。

此前检查点自动验证（2026-09-10）：全仓 246 文件、1745 项通过，2 项条件测试跳过；Git 合并 View 的 36 项回归含点击提示、500ms 延迟及取消、普通分支标签和不拦截选择。类型、110 份文档、260 个源文件的导入边界及 diff 检查通过；共享 bundle 验收含真正 Wing 结果区及 Preview/Host/文件算法隔离。`pnpm ext:dev:prepare` 完整通过，Auto 6 个 Wing 包来自并列源码、consumer node_modules 命中 0；CAD 按其独立 Registry 入口通过，不称为 CAD 本地 Wing 验收。正式页面内存夹具的浏览器检查确认 remote 点击开关、普通分支标签和未勾选状态；不代替真实 Git 操作验收。0.9.0 归档 VSIX SHA-256 仍为 `6666b974464f32051acca5fedb09a224a138c4b006ee7bca097be06641b4bddd`。

此前阶段交接：使用用户已整理的 Wing `develop@719b6b78fa`（包版本 0.7.3）执行 `pnpm ext:dev:code`，曾打开 `kt-auto-code-local-host-eOhOWJ` 独立快照，包含五项正式 Primary、长提交说明截断、分支切换失败对话框、remote 点击详情及提示延迟；正式依赖锁未改。该快照现已被最终候选取代，最新 Host 与验收边界统一见 [内测收尾](0.9.1-内测收尾.md)。

此前开发归档：按用户要求在 `v0.9.1` 分组提交，`e9d57df` 为五项 Primary，`b3d2e9d` 为 Git 修正与提示交互，`f16a99c` 为文档检查点。主仓当时为用户的 `test1`，未在主仓提交或移动其分支；该检查点包版本为 0.9.0，当时未重新打包、推送或发布。本轮 0.9.1 版本与制品状态统一见 [内测收尾](0.9.1-内测收尾.md)。

收尾追加：修正自动代码 JSON Right 高度链导致的垂直滚动失效，预检详情改按实际内容区计算高度并跟随尺寸变化；Right 激活时恢复对应 JSON 的自动代码 Primary，不触发扫描或抢编辑焦点。保留一 JSON 一 View 的独立状态与既有详情最小高度，正式窄/矮视窗点检单列记录。

2026-09-10 用户重启实测确认：整体上下滚动、下方内部滚动及其他原有滚动锁定行为均已恢复。该确认针对滚动修复，不代替随后新增的单行 Header 整合或跨 JSON 焦点验收。

前一候选交付：Auto `3cecd45` / Wing `ec38785` 固化生成规则 `1.0.0`、单行 JSON Header 与预检单 Block Header。完整本地构建中 Auto 248 文件、1772 通过、1 条件跳过，真实 Extension Host 15 项代表流程及 VSIX 门禁通过；0.9.1 包与回执已独立归档。规则升级约定写入两仓 AI 指引；结束标记缩进仍仅记录。

最后收口追加：Wing `cfb0cf5` 统一参数表与预检结果的 Block Header 字体、间距、箭头和折叠范围，保留工具独立操作与 Body 滚动；真实共享 bundle 的浏览器标题/键盘/筛选点检和 148 项 Wing Codegen 回归通过。纯 UI 修改不升级生成规则。JSON Right 标签改为文件名（同名不加目录）明确留至 0.9.2，与 Git Right 原型一起记录，不作为本版阻塞项。最新源码、包哈希、独立归档与未验项统一见 [内测收尾](0.9.1-内测收尾.md)，不代表公开发布或全部人工验收完成。

附：按用户要求，Wing `codex/cleanup-components` 的干净 worktree 从 `d6d2f36` 迁到 `719b6b78fa`，未删除 worktree/忽略文件。旧提交保留于 `codex/archive-cleanup-before-719b6b78fa-20260910` 和该 worktree 的 `dist/archives/cleanup-before-719b6b78fa-20260910/history.bundle`（完整历史验证通过）。主 Wing 的用户 `docs/plan.md` 未改，未推送。

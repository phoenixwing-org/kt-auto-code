# 0.9.0 Wing 控件与自动代码整改目标

状态：current

Owner：KT Auto Code / Phoenix Wing maintainers

适用版本：KT Auto Code 0.9.0、Phoenix Wing 0.7.3 开发线

最后核验：2026-09-09

## 当前收尾状态（2026-09-09，内测候选已验证）

以下为当前源码状态；后续各日期小节保留阶段历史，其中“仅 Preview / 待接线”和旧包哈希不覆盖本节，也不能作为新候选包证据。

- **自动代码正式正确性已补齐**：保存绑定序列化快照与 revision，同 URI 的保存/重载串行并在写前、写后核对会话身份，迟到回执不能把后续草稿标 clean；表格 checkpoint 不重建当前输入与焦点。预检捕获输入版本，编辑、外部变化或取消后拒收旧计划；已应用结果只允许查看源码，不恢复执行资格。Primary“全部应用”、Right“应用”的范围保持不变。
- **checkpoint 制品门禁已实现**：本地 Wing Core 与实际打包 Table JS 均通过行为断言验证“保存旧快照后仍保留新草稿”，不只检查导出名称或字符串。与既有 Run 清理运行时、Wing 来源和 local receipt 门禁配合；旧 Registry 缺少能力必须失败，不允许静默回退。本地内测候选不等于 Registry / Marketplace 可发布。
- **头文件与 Git 共享原型、正式接线已落地**：头文件 Primary 复用共享组件及紧凑 Ignore，目录推导/选择/编辑经 typed companion 动作进入原 Controller；Right 去掉重复目录与工程环境入口，保留预览、写入、完整结果与诊断。Git 复用正式 Primary / Tree，保留登记、切换、刷新、移除、递归搜索/停止与按需 commit；只移除用户明确要求删去的初始化入口和空态说明。
- **新增头文件服务烟测已接入回执**：隔离夹具覆盖映射根/工程根双根 Ignore、预览不写盘、旧源码指纹整批拒绝、实际修正 2 文件 / 3 处、复扫零命中及忽略文件字节保留；定向 2 文件 / 59 项通过。该证据是服务与门禁回归，不冒称最新 Extension Host 已运行或视觉已验收。
- **0.9.0 停止线**：只收口本轮明确范围、回归和人工内测候选；不再重做已初步认可的页面。C++ 成员排序、头文件 ASCII 修正、编码修正、UUID 替换、CAA UI 的 Primary 整改留给 [0.9.1 计划](0.9.1-代码辅助Primary整改计划.md)，不提前实施；后续 TS provider / export 继续按独立计划推进。
- **最终自动门禁通过**：232 文件 / 1533 通过 / 2 条件跳过；此前本轮原生 CMake 另 35 项、真实 Wing 清理 DOM 另 3 项通过。最终 `pnpm ext:dev:prepare`、单仓及 Auto+CAD 真实 EH、新 VSIX 与 receipt / SHA-256 均通过；头文件 EH 实写 2 文件 / 3 处并显式复扫零命中。最新候选 `6666b974…41b4bddd`、路径和证据见[内测说明](0.9.0-内测说明.md)；只供内测，正式发布待[冻结清单](0.9.0-发布冻结清单.md)的 Wing Registry 门禁。
- **最后 UI 增量已同步正式**：头文件保留顶部提示，删表格前重复标题/统计；Primary 独立结果 Block，Right 固定状态/操作列及“打开”，复用现有点行定位。写入成功保表并标已写入，异常待核对，不自动重扫；源码 `0f75037`，定向 56 项通过。Git 以 Combo 为唯一简报上下文；CAA Combo 单行通知修复与 139 项 Wing 回归通过，原问题文档已压缩。
- **目录与关闭联动已收口**：源码 `be4079e`，头文件仅新建 Right 时取当前目录；三个任务型工具（项目改名、编译工具、头文件）的最后一个 Right 关闭时同步关闭 Primary/Open Item。自动代码按 JSON 管理除外；不改 Primary 关闭的反向边界。用户已确认头文件样式/状态列及目录两场景，新增关闭联动和完整主题/多实例仍留人工点检。

## 2026-09-09 内测收尾补充

- 打包安全复核已补齐：Git 预览与执行统一双 force，冻结未跟踪/ignored 完整树、独立暂存区 diff，并拒绝旧版不完整快照；不跟随目录链接。AutoBuild 清理对话框取消已接到 Host，撤销 token、阻止延迟预览复活和后续删除，不停止无关构建。reset 后停止/失败保留阶段日志；取消不承诺回滚或打断已启动的 Git 命令。
- 已交付安全修订包中的 Run 仍是旧入口：build/objects/obj 为递归匹配；Git 是 `clean -dfx`（包含 ignored、不 reset）。下述包后 Run 阶段不能反向当作该安装包已包含的能力。
- 自动代码共享 Primary 阶段已提交：Wing `9599f17`、Auto `b615c45`。Right 的 JSON 编辑/预检语义仍待后续整改，不因打包而宣称整轮完成。
- AutoBuild 的整批已存在仓库更新、项目行独立更新和标准 CMake 已改走 TS；Primary 新增 Debug/Release 并持久化。迁移状态与未覆盖能力见[运行时计划](编译工具/TypeScript运行时迁移计划.md)。
- Preview “结束任务”原先是静态 disabled 按钮；已恢复模拟点击反馈。捕获阶段兜底日志覆盖 Shadow DOM、阻止冒泡、点击后移除的按钮，有业务日志时不重复；正式结束任务门禁未删除。
- `export.yaml` 仅登记[独立讨论文档](编译工具/Export跨平台支持讨论.md)，按用户要求稍后研究。
- 用户授权准备 0.9.0 人工内测包，不授权 Marketplace 发布。包的范围、遗漏与测试清单见[内测说明](0.9.0-内测说明.md)。本目标继续进行，Run 清理整合、自动代码 Right 与迁移计划的未完项不隐式归档为完成。

## 2026-09-09 包后进展：Run 统一清理接线

- 按用户最新确认：Run 首行采用“刷新 / 清理”文字按钮，右侧保留“仅当前系统”；Git 清理树叶子移入首行“清理”对话框，默认直达 git-untracked。原 build / objects / `*.obj` 三个快捷叶子恢复原树样式，单击直接清理、不询问，仍递归匹配。Git 仍为单 force 的未跟踪与 ignored 清理，不执行 reset，不删除嵌套仓库。
- Wing 新增独立的递归产物与 Git 未跟踪 preview/execute API；不改变 AutoBuild 直属规则和强制恢复的默认语义。冻结目标树、目录身份与祖先路径；Git 同时核对完整暂存区。符号链接与嵌套仓库边界明确跳过，结果日志不把保留的父目录报成已删除。
- Auto Host 持有不可复用的 session/revision/preview token、工作目录与模式指纹；执行前后核对信任、目录和 Task 并发。三个快捷项仅在内部扫描/冻结后立即执行，不请求打开确认框；显式“清理”对话框先预览，再点击清理。用户明确删除 Run 的额外风险勾选行，`requireHighRiskConfirmation: false` 不改变其他消费者默认确认行为。取消撤销后续步骤，等待 provider 退出后才释放互斥；不回滚已完成删除，不中止无关 Run 任务。
- Preview 的 Run 与 AutoBuild 现均使用 Wing 清理控件，仅模拟数据与日志，绝不执行文件或 Git 删除。AutoBuild 手写清理弹窗已删除，保留三模式及其原有确认语义；见下方继续点检记录。
- 本轮新增能力不在 SHA-256 `a162e52d20cf1c1750dddce691ea01e00a4e07d5352e96ed8ce9c696672d9fef` 的已交付包中；独立分发目录保持不变。真实深/浅主题、焦点与可见 Extension Host 人工点检待补，本轮机器锁屏时未宣称视觉验收通过。
- 详细迁移、安全边界与验收记录见 [Run 实现基线](运行模块/README.md#09-包后增量run-统一清理)。自动代码 Right 和后续 TS provider 仍未完成。

## 2026-09-09 UI 接线与回归补充

- Right 目录说明成为 `KtcRightViewShell` 默认规则，独立于 Catalog 标题；正式 Package、Rename、AutoBuild、Codegen 读取各自会话/配置路径。Preview 四个 Header 已同步。空路径不猜测；同值模型更新不重建 Shadow DOM。
- AutoBuild 配置首行按最新确认使用五个文字按钮，状态在最近配置 Combo 下；覆盖此前纯图标方案。头文件 Primary/Right 示例与紧凑 Ignore 组件目前仅 Preview 接线，详见[独立计划](头文件引用修正Primary整改计划.md)。
- 项目改名底部只读目录原型已获用户确认；正式 `chooseRoot` 去重和目录固定另列 TODO，不改变全局目录或 Primary `×` 语义。各页面状态统一见[UI TODO 备忘](0.9.0-Right工具Primary迁移点检.md)。
- 回归发现并修复 Git 进程 stdin 的偶发 `EPIPE`：无输入命令不再写空管道；有输入命令捕获独立流错误并等待写入与退出，失败不能报成功。没有修改 Git 改写算法或确认流程；增加 10 项确定性流失败测试。
- 包后 UI 批次（自动代码 Right 新原型接入前）：221 文件 / 1363 通过 / 2 条件跳过，无未捕获异常；Wing run-node 47、code-core 103 通过。macOS 原生 CMake 可选测试另行启用并通过 35 项；Git Wing 图联调可选测试仍未在本轮启用。
- 文字工具栏等当前正式代码已重新通过 `pnpm ext:dev:prepare`；此前真实 Extension Host 代表流程 smoke 通过，不代替新按钮和主题的人工验收。本次浏览器复核受 Mac 锁屏影响，未宣称通过。
- Wing Run 运行时阶段已提交 `8c2affe`；Auto 消费端归档不得修改正式 manifests/lockfile 或覆盖已交付 VSIX。

## 2026-09-09 自动代码 Right 内存原型接线

前一批 Run/已确认 UI 已提交 `9e09d0c`。本阶段替换原型占位，不改正式文件 I/O：

- 单个 `createPreviewCodegenSurface` 同时驱动 Primary/Right，两份 JSON 按 URI 保留文档草稿、checkpoint、控制符选择与结果；旧静态 Primary adapter 已移除。
- 复用 Wing Table（17 列、9 个行操作）、ControlPanel、PrimaryPanel 与 Auto DocumentModel/ControlSessionController；预检真实调用 Wing Analyze，Apply 调用纯投影并只更新内存源码。无当前计划先预检，编辑/取消使旧计划失效。
- 支持 JSON/CSV 内存输入、保存/重载、预检结果筛选/详情、查看内存源码、END 文本反馈、批量模拟与报告。导入后候选使用文档 URI+路径复合 ID，避免串文档；批量进度/报告跟随实际执行文档。
- 18 项真实 DOM 回归通过；临时服务的实际 bundle/HTTP 200 已验证。随后浏览器完成内存预检及浅色选中行点检；整页仍待用户确认，正式外部变更/删除、写盘/回滚、磁盘缓存、Host 批量锁、多 Editor 生命周期及新 VSIX 仍未据此验收。阶段提交 `d601935`。
- 本阶段全量 221 文件 / 1380 通过 / 2 条件跳过，typecheck、107 份文档检查与 `pnpm ext:dev:prepare` 通过。异步批量测试已改为等待完成状态，不用固定延时猜测完成。
- Wing 参数表默认九按钮统一为简短文字，actionId/title/ARIA/顺序/可用性不变；26 项表格测试及包构建通过，阶段提交 `3e31ba2`。用户的 `docs/plan.md` 未包含在提交中。
- 用户随后提供参数表工具栏截图并确认“好像可以的，辨识度提高”；本版九按钮文案保留。确认仅覆盖该按钮条，不扩展为整个 Right 或真实写盘已验收。
- 当前 Auto dist 再次验证嵌入四个 Run 清理真实函数；已交付内测 VSIX 的 SHA-256 仍为 `a162e52d20cf1c1750dddce691ea01e00a4e07d5352e96ed8ce9c696672d9fef`，本阶段没有覆盖旧包或发布新包。
- 已交付 VSIX 再次校验仍为 `a162e52d20cf1c1750dddce691ea01e00a4e07d5352e96ed8ce9c696672d9fef`；这些增量没有重打包或发布。

## 2026-09-09 自动代码主题与正式图标

- 用户确认部分浅色主题正常，但 `Light (Visual Studio - C/C++) / Visual Studio Light - C++` 的蓝色选中行出现灰色副文字。最新方向覆盖早期截图配色金样：保留布局，去掉干扰主题的自定义配色，不固定背景或按主题名称分支。
- Wing Primary 的路径/状态徽标、Right 的编号/元信息继承选中行前景色；普通行颜色与交互不变。Preview 删除自动代码专属深色颜色覆盖，两侧使用同一套 VS Code 主题变量。
- 浏览器浅色实测两处蓝底 `rgb(0, 96, 192)` 下主/副文字及徽标跟随主题为白色；随后补查浅色、深色、高对比截图并恢复浅色。Wing 130 项测试、Auto 全量 221 文件 / 1380 通过 / 2 条件跳过，typecheck、107 份文档检查及 `pnpm ext:dev:prepare` 通过。Wing 阶段提交 `6c421ff`，用户 `docs/plan.md` 未包含。
- 正式 Ribbon 原先收到裸 `sliders` 字符串，未加载已有三滑杆 SVG；现接入 `media/tools/codegen.svg` 的既有 Webview URI 链路，Catalog 语义与导航结构不变，29 项相关回归通过。
- 已运行 `pnpm dev`，启动日志生成新快照 `kt-auto-code-local-host-XzNWPZ` 并发起可见 Host；随后查询 VS Code 窗口时 Mac 已锁定，未完成具体主题的实际 Host 视觉验收。旧窗口未关闭，已交付 VSIX 未更新。
- 用户随后提供正式 JSON 行与预检结果行蓝底白字截图，明确确认“效果通过了。正式的”；本主题两处选中态转为已验收，不代替完整交互和新 VSIX 验收。正式 Ribbon 三滑杆也已在用户随后导航问题截图中可见。

## 2026-09-09 继续点检

- 用户初步认可自动代码、搜索替换、项目改名、Run、编译工具的 UI/正式一致性（“95 以上 OK”），不是功能覆盖率。0.9.0 优先收尾头文件引用与 Git，再完成回归和制品边界核验；不再扩大已认可页面的布局调整。其他仅 Primary 的代码辅助工具已另列 [0.9.1 计划](0.9.1-代码辅助Primary整改计划.md)，本轮不实施。
- 用户指出正式 Toolbar 点击组外独立工具仍保留组二级区，原型无此问题。Host 现统一收起组外导航，保留组内叶子、MRU、工具与 Right 会话；投影签名纳入组展开状态，避免目录与父按钮高亮不同步。89 项导航相关回归通过，正式新版人工点检待补。
- Git 原型与正式共用 Primary 面板：空状态删初始化入口与旧说明，递归搜索/停止移入 Tree，仓库登记/刷新/移除保留；默认显示 2 条 commit，其他归入“更多 commit”。17 项定向测试与浏览器模拟实操通过，未执行真实 Git 写入；最新正式 Host 仍待本轮统一构建点检。
- AutoBuild Preview 已替换手写清理弹窗为 Wing `PnwCleanupDialog`：规则、Git 强制恢复、CMake、ROOT/工作目录、mode 直达、规则缓存、预览失效与取消均保留。AutoBuild 的 Git 高风险勾选保留；Run 独立不确认要求不扩展到这里。11 项 adapter 回归，以及 `pnpm exec vitest run --config scripts/vitest-local-wing-cleanup.config.mjs` 的 3 项真实 Wing DOM 回归通过；固定内存清单不代表真实 YAML 匹配结果。
- 真实 Extension Host 补查磁盘缓存复用、按文档 URI 分离、旧生成器/损坏缓存重建、源码改删后旧计划拒绝、索引更新，以及 JSON 删除后普通保存拒绝/显式重建/重新出现文件保护。单仓与 Auto+CAD 隔离 smoke 均通过，保留回执分别位于 `/tmp/ktc-eh-sTieb8`、`/tmp/ktc-cad-eh-G28oTu` 的 `workspace/.phoenix/extension-host-smoke-v1.json`；仅改临时夹具，不代表桌面逐按钮验收。
- 单仓与跨仓 smoke 启动器共用完整回执门禁；新增 3 个 flow、11 个持久化证据必须为 true，40 个反例/有效例测试通过。跨仓不再仅检查两个扩展 active 而跳过 Auto 交互证据。
- 用户最新要求区分应用范围：Primary 显示“全部应用”，Right 显示“应用”，原 `applyAll` / `apply` action 与任务范围不变。

## 本轮目标

1. 将本仓 `PnwCombo` 的可管理条目能力迁入 Phoenix Wing，保留分组、逐行删除、全部清空、键盘操作、视口定位和上下自动避让；KT Auto Code 改为消费 Wing 导出，不保留两套实现。Wing 阶段提交：`2b647d6`。
2. 在 Wing 提供 Host-neutral 的统一清理对话框，以及可由 Node Host 复用的预览、冻结、复验和执行能力。Wing UI / 初始 Node 阶段提交：`2b647d6`；目录内容冻结清理补充提交：`80755c0`；方式切换时同步规则区与适用目标：`4bab90a`；`showModal(modeId)` 按方式直达：`0b9d970`。
3. AutoBuild Primary 的“执行”按钮组增加 **清理**；点击后打开统一对话框。Primary 维护区不再常驻“清理仓库”和“手动清理 Root”两个块。
4. 清理范围覆盖 **ROOT_DIR** 与 **工作目录**；清理模式覆盖 YAML 规则清理、CMake 构建目录清理，以及显式高风险的 `git reset --hard + git clean -ffdx`。
5. 完成“自动代码”原型与正式 UI / 真实交互的一轮整改。任何原有按钮或能力若未明确要求删除，必须保留、迁移或登记为待接入，禁止静默消失。
6. 梳理 Git Primary 的空状态和仓库管理按钮；移除与 VS Code 原生能力重复的“新建 Git 仓库”，保留插件独有的递归发现、仓库切换、刷新和已登记仓库管理能力。
7. 点检集成 Toolbar 中竖向 `CODE` rail 的框架语义；若 Phoenix Wing Ribbon 已有分组折叠能力，则让该 rail 成为可访问的折叠/展开触发区并复用同一状态，不新增与现有 Toolbar chevron 冲突的第二套状态。
8. 扩展 AutoBuild 的 `BUILD_MANIFEST.json` 使用链路：允许把清单中的 Git 地址先导入为构建草稿，
   不要求目标仓库已存在；后续统一执行时对缺失目录执行检出，对已存在仓库执行更新，并继续进入依赖它的
   link / export / build 节点。

Primary 外壳不在本轮变更范围内；结构、顺序和关闭语义以已确认的[前端规则](前端开发规则.md)及当前任务指令为准，不由本文旧名称推导改动。

## 清理能力迁移清单

| 原入口 / 能力 | 新位置 | 状态 | 处理决定 |
| --- | --- | --- | --- |
| AutoBuild 执行区：脚本 | 原位置 | 保留 | 不受清理迁移影响 |
| AutoBuild 执行区：预检配置 | 原位置 | 保留 | 不受清理迁移影响 |
| AutoBuild 执行区：启动 | 原位置 | 保留 | 不受清理迁移影响 |
| AutoBuild 执行区：停止 | 原位置 | 保留 | 不受清理迁移影响 |
| AutoBuild 维护区：清理仓库 | 执行区 **清理** → 统一对话框 → Git 强制恢复 / CMake 清理 | 原型与正式 Host 已迁移 | 能力保留；旧常驻行和 Windows-only PS1 调用入口已删除 |
| AutoBuild 维护区：手动清理 Root | 执行区 **清理** → 统一对话框 → ROOT_DIR / 工作目录 / YAML 规则 | 原型与正式 Host 已迁移 | 能力保留；旧常驻 YAML 块已删除，规则继续缓存到当前 AutoBuild JSON |
| Run：删除 build 目录 | 原清理树叶子 | 原型与正式 Host 已接线，包后增量 | 保留递归；单击直接执行，不确认 |
| Run：删除 objects 目录 | 原清理树叶子 | 原型与正式 Host 已接线，包后增量 | 保留递归；单击直接执行，不确认 |
| Run：删除 `*.obj` | 原清理树叶子 | 原型与正式 Host 已接线，包后增量 | 保留递归；单击直接执行，不确认 |
| Run：清理 Git 未跟踪及 ignored 文件 | 首行“清理” → 对话框默认 git-untracked | 原型与正式 Host 已接线，包后增量 | 树入口迁移，能力保留；单 force clean 保留 tracked / 嵌套仓库，不 reset |
| 脚本同步 | AutoBuild 维护区 | 已保留 | 仍同步 ROOT/tools 与 ROOT/sample，不并入清理弹窗 |

## 统一清理对话框约束

- 组件只接收 Host 投影并发送语义事件，不直接读取 VS Code API、配置文件或工作区状态。
- 必须显示目标类型、绝对路径、规则/动作摘要、预计命中数量和精确预览；危险等级不能只靠颜色表达。
- YAML 规则清理默认只处理目标目录的直接子项；目录树快照不跟随符号链接，确认后只删除冻结且复验一致的目标。
- Run 递归模式使用单独 API，不能改变上述默认；匹配目录中包含链接/嵌套仓库时保留边界，只冻结可安全清理的子项。
- Git 强制清理必须单独标为高风险，明确展示 `git reset --hard HEAD` 与 `git clean -ffdx`，不可借普通“清理”确认静默执行。
- 工作目录和 ROOT_DIR 必须分别投影；路径缺失、文件系统根、不可访问路径和确认后身份变化一律拒绝执行。
- 组件可被 AutoBuild、Run 和其他插件复用；业务配置保存、Output 日志和生命周期取消由 Host 负责。
- 调用者可通过 `showModal(modeId)` 直达指定清理方式；组件切换方式时负责同步规则区可见性、适用目标
  默认选择和旧预览失效，Host 不重复实现这些交互。

## AutoBuild PowerShell → TypeScript 边界

子 agent 已完成只读审计，并结合真实 Root / 项目脚本完成复核。详细阶段、冻结语义和验收门禁见
[《编译工具 TypeScript 运行时迁移计划》](编译工具/TypeScript运行时迁移计划.md)。当前决定如下：

### PowerShell 兼容策略

- 插件 UI 的目标运行时不再依赖 `Invoke-AutoBuild.ps1`。Git 更新、标准 CMake、linkCAA、已知导出和
  CAA 命令编排按阶段迁入 Wing TypeScript。
- 项目自带 `mk.ps1`、`export.ps1`、`linkCAA.ps1` 继续作为兼容入口；只有无法表达的自定义逻辑才
  显式选择“项目脚本” provider，不再把 PS1 当成标准 CMake 或 linkCAA 的默认前提。
- CAA/MSVC 最终仍调用 Windows RADE/MSVC 的 `.bat/.exe` 工具链；TypeScript 可直接编排这些命令，
  但这不会让实际 CAA 编译自动获得 macOS/Linux 支持。
- “构建脚本”和“仓库检出”的保存/导出继续生成 Windows PowerShell 5.1；当前不新增 `.sh`。
- `Invoke-AutoBuild.ps1`、`Functions-Cleanup.ps1` 与 sample 继续作为独立部署、示例和回退工具，
  在相应 TS provider 通过验收前不提前删除。
- “版本归档”是 JSON，不归入脚本平台限制。

### 可迁移到 TS / Wing

| 能力 | 归属 |
| --- | --- |
| 配置校验、路径规范化、任务选择与 DAG / 失败跳过状态 | Wing `run-core` |
| Git 探测、更新和强制清理；目录探测、复制、hash、进程执行、流式日志 | Wing `run-node` |
| Root / 工作目录规则清理和 CMake 产物清理 | Wing `run-core` 规则 + `run-node` 执行 |
| 统一清理弹窗与 preview token / 高风险确认 | Wing UI |
| VS Code 弹窗、OutputChannel、Task、workspaceState | KT Auto Code Host adapter |
| AutoBuild 配置到具体任务的业务映射、ROOT/tools 同步布局 | KT Auto Code 产品层 |

迁移优先级固定为：**仓库同步 → 标准 CMake → linkCAA/linkOut → 声明式 export → Windows CAA 直接 runner**。
每一阶段先增加 Wing provider 和 Auto adapter，再切换默认调用，最后才删除对应的插件 PS1 运行路径。

### “运行”失败继续规则

1. TS 校验、目录解析和项目探测始终执行；每个 Git 仓库独立产生结果。
2. ROOT / 3rdParty 失败时跳过依赖它们的 link/export/build；普通项目 Git 失败只跳过该项目链。
3. `linkCAA.ps1` 或某项目 `export.ps1` 失败，只跳过其依赖项目；其他项目继续。
4. CMake 项目可独立并行；保持当前正式语义：任一 CMake 失败，本轮全部 CAA 标为“已跳过”，
   待建立项目级依赖后再缩小范围。
5. CAA 项目失败不影响其他已就绪 CAA 项目。
6. PowerShell 不存在或无法启动时，PS 节点标为“不可用 / 已跳过”；TS 探测、Git 状态刷新、
   汇总和日志继续。不得因此自动触发清理或脚本同步。
7. 用户停止后不再创建新任务，终止运行中的 PS；未启动项标为“已取消”，不是“失败”。
8. 错误以进程启动失败或退出码判定，不能按 stderr 是否有文本判定。

正式状态模型还需增加 `skipped / cancelled` 和依赖关系；当前单体
`Invoke-AutoBuild.ps1 -SkipBuild` 失败即中断的路径在新 DAG 就绪前继续保留，不能先删回退链。

### 原有执行能力保留表

- 配置：打开、保存、另存、关闭、详细配置、最近配置。
- 执行：脚本、预检配置、启动、停止、并行编译、Output、新增统一清理。
- Right：预检、运行/停止、ROOT / 3rdParty 更新选择。
- 项目表：选择目录、探测当前目录、移除未启用项、探测列，以及逐项目启用、分支、更新、
  CMake、CAA、linkCAA、探测、运行、移除。
- 任务行单步运行；脚本对话框三个页签和目标目录选项；ROOT/tools 与 ROOT/sample 同步。

### `BUILD_MANIFEST.json` 导入与检出 / 更新

- 现有 schema v1 继续代表一次成功构建后的版本归档；`finishedAt`、`status`、完整 Commit 和
  `dirty` 是当时事实，导入后不能当成当前探测结果。
- “项目与仓库”Header 增加短按钮 **导入…**；系统文件选择器接受清单后，仅显示“新增 / 更新 /
  忽略”的简短汇总并由用户确认合入草稿。正常情况不显示明细表；只有 Origin 冲突、目标重名或
  格式错误才展开问题项。导入不访问 Git、不要求目标目录存在，也不直接检出、更新或构建。
- 仓库以规范化 Origin 去重。`root`、`thirdParty` 分别映射到 ROOT_DIR、ROOT_DIR_3rdParty；
  `project` 映射为项目表行，保留 branch、commit 和 buildKinds。
- schema v1 没有目标相对目录：普通项目默认建议为 `工作目录/<name>`，必须在导入预览中明确显示，
  并允许用户在执行前修改。不得仅凭当前机器上恰好存在的同名目录静默绑定。
- 统一执行前逐条探测：目标目录不存在时计划为“检出”，存在且是同 Origin Git 仓库时计划为“更新”，
  存在但不是 Git 仓库、Origin 不一致或目标路径冲突时停止该仓库并要求处理；其他独立仓库继续。
- 指定完整 Commit 时先 clone/fetch，再检出该 Commit；未固定 Commit 时按 branch 检出或
  `pull --ff-only`。Root / 3rdParty 继续执行 LFS 与递归子模块步骤。
- 这项先登记为 AutoBuild 后续阶段；正式 UI、消息契约和真实执行未完成前，不把它标为已支持。

Primary 旧常驻清理块及其 `cleanRepositories / cleanRootArtifacts` Controller action 已在替代链路和
自动测试就绪后删除；清理能力本身没有删除。隐藏代理 substrate、无调用者的旧 `exportLauncher`
和过期 `clean` 字段仍未清理，须单独证明没有消费者。Primary/Right 双入口、单项目/单任务运行、
脚本同步/脚本导出、Git untracked/强制恢复、JSON 归档/脚本导出均不是重复能力。

## 自动代码按钮 / 能力点检基线

2026-09-09 已核对共享组件、Host 消息映射和 Controller。下表的“已接线”表示代码链路存在，
不等同于这轮真实桌面逐按钮验收。除非用户明确要求删除，否则不得移除。

| 原有能力 | 原型位置 | 正式位置 | 当前状态 |
| --- | --- | --- | --- |
| 打开 JSON 配置 | Primary 操作按钮组“打开” | 同一个 Wing 组件 → `openJson` → `pickJson` | 已接线；文字按钮已下沉共享组件 |
| 导入 CSV | Primary 操作按钮组“导入” | 同一个 Wing 组件 → `importCsv` | 已接线；保留转换及冲突处理 |
| 应用全部 | Primary 操作按钮组“全部应用” | 同一个 Wing 组件 → `applyAll` → `applyAllV1` | 最新可见文字、tooltip/aria 均明确“全部应用”；与 Right 当前 JSON 的“应用”区分 |
| 刷新配置 / 取消刷新 | Primary“刷新”，运行中“取消” | `refresh / cancelOperation` → workspace operation coordinator | 已接线；取消保持可用 |
| 扫描控制符候选 / 取消扫描 | Primary“扫描”，运行中“取消” | `scanCandidates / cancelOperation` | 已接线；取消保持可用 |
| JSON 配置列表与未保存状态 | Primary 管理区 | `openDocument`；dirty/外部变更徽标 | 已接线；完整路径有 tooltip/aria |
| 四个文档属性 | 当前 JSON 折叠块 | Prefix/Middle/Namespace/Append → `updateMeta` | 已接线；操作中锁定 |
| 应用报告与打开报告目录 | Primary 管理区 | `openReport / openReportDirectory` | 已接线；报告详情另有过滤与定位操作 |
| 控制符目录 / 候选 | Primary 管理区；原型已按 Wing 元数据补齐 32 项 | Wing 32 个控制符、分组/范围/状态筛选、单项/可见输出；`openCandidate` | 正式已接线；原型分组选择/输出有模拟反馈，预检前不显示伪造命中 |
| 选择模板 | 旧原型占位已删除 | 无同名正式 action | 替换为真实 JSON 参数编辑形态，不算删除正式能力 |
| 生成预览 | 已替换为 Right“预检” | 正式是“预检”及生成产物预览 | 原型实际 Analyze 内存源码；不把预检当成 Apply |
| 生成器选择 / 目标目录 | 旧原型 Phoenix Web / CAA Command 与静态 target 已删除 | 正式由 JSON 参数、控制符和命中源码决定 | 删除无正式 action 的占位内容 |
| 参数表与行操作 | Right 内真实 Wing 17 列表格 | JSON View 内同一 Wing Table | 原型已接：排序、复制/粘贴、插入、副本、移动、删除及折叠/状态栏；不复制算法 |
| 表格自适应列宽 | Right 共享表格内 | 参数表自适应工具与共享表格能力 | 同组件能力保留；实际窄宽视觉待补 |
| 保存 JSON / 重新加载 | Right 文字按钮，读写内存 checkpoint | JSON View → 保存/重新加载与冲突保护 | 原型可验证草稿/保存语义；不证明真实磁盘冲突处理 |
| 预检 / 预检结果 / Apply | Right 操作行与共享结果区 | JSON View 与预检结果块；查看源码、复制 END、诊断与产物详情 | 内存 Analyze/Apply/结果反馈已接；正式布局同步与真实写盘验收另行推进 |
| 取消预检 | 原型预检按钮运行中切“取消预检” | 运行中取消与 revision/abort 门禁 | 内存异步撤销回归通过；正式能力保留 |
| 多文档、外部变更与旧计划失效 | 按 URI 保存各文档草稿和 ready/applied/stale；批量仅模拟 | 按 URI 复用 JSON View；changed/deleted、批量锁与保存冲突 | 原型两文档切换/陈旧计划已测；外部变更、磁盘缓存、真实 batch 锁、多 Editor 实例待独立验收 |
| 复制运行诊断 | 原型无按钮 | 命令面板 `ktAutoCode.codegen.diagnostics` | 命令入口保留，不是失踪按钮 |

### Right 细项回归（2026-09-09）

正式 `src/tools/codegen` 48 文件 / 205 用例通过。新原型需按下列细项对齐；大类表不等于逐按钮验收：

- 参数表：折叠、17 列类型编辑、行选择与状态栏；`自适应 / 排序 / 复制 / 粘贴 / 插入 / 副本 / 上移 / 下移 / 删除`及首尾、空表、无选择、无剪贴板禁用语义。
- 预检：运行中取消；结果三筛选（命中/问题/全部）、显示路径、首次结果自动展开；未预检、缓存、已应用、过期各状态。
- 结果：区域产物/诊断详情、查看源码、missing-end 的复制 END；列表/详情分隔条鼠标与键盘调整、会话比例恢复。
- 文档：dirty/changed/deleted、保存中门禁；Apply 无缓存先预检，旧 revision/源码指纹复验；表格草稿在防抖、隐藏和卸载前同步。
- 多文档/批量：按 URI 保留会话、草稿与结果；批量进度遮罩和 Host 消息门禁。内存样例不能证明真实外部文件冲突或写盘安全。

旧原型的“选择模板 / 生成器 / 静态目标目录”没有对应正式 action，替换它们不算删除正式能力；已明确移除的旧选择菜单不恢复。

早期阶段 `2ef2d4a` 曾按深色截图配置选中 JSON 行配色。用户随后确认不同主题的蓝底灰字问题，
现以“跟随 VS Code 主题”覆盖该配色基线：背景/边框使用主题变量，选中路径和徽标继承选中字色；
不再为自动代码保留专属深色覆盖。普通行的次级文字层级不变。

本轮删除的是 Preview 的 `applyPreviewCodegenActionLabels` Shadow DOM 补丁：已认可的
“打开 / 导入 / 应用 / 刷新 / 扫描”直接进入 Wing `KtCodegenPrimaryPanel`，窄栏允许按钮换行，
重绘无需再次替换文字。业务 action 未删除。批量应用期间，JSON、报告、候选和报告目录按钮都
遵循 operation lock，避免键盘绕过视觉遮罩；操作结束后恢复。选中样式按上述最新主题规则适配。

已明确删除的旧选择入口另有历史证据：
[控制面板滚动筛选点检表 A6](codegen-plan/Codegen控制面板滚动筛选点检表.md)记录用户删除
“选择工具”及全选/全不选/单选菜单、展开缺失模板；当前保留行/分组 checkbox、筛选和输出。
因此不把这些菜单当成本轮意外丢失，也不重新添加。兼容单选状态和预设函数仍在领域/会话层。

本轮证据：Wing 共享 Primary DOM/操作锁/Host 边界 12 项测试通过，Codegen 包 typecheck 通过；
Auto 侧 Codegen、Sidebar 和原型相关 51 文件/239 项通过，新增原型目录覆盖与样例 2 文件/4 项通过，
Auto typecheck 通过。临时本机预览服务实际打包并返回 HTTP 200，已移除文字补丁且包含完整目录。
`pnpm ext:dev:prepare` 通过，6 个 Auto Wing 包来自并列仓库，CAD 50 项测试和构建通过。
真实 VS Code 1.136.1 Extension Host 烟测通过打开、预检、冲突保护、应用、保存重载及回滚，
夹具中 2 个候选文件、2 个区域、1 个实际改写文件；烟测临时目录已按脚本回收。

本轮桌面逐按钮视觉点检因 Mac 锁定尚未进行；上述自动证据不能代替深色/浅色/高对比和
窄 Primary 的人工验收。完整目标仍未完成，原型 Right、后续 Run 清理与 AutoBuild TS 阶段继续保留。

## Git 按钮 / 能力点检基线

| 原有入口 / 能力 | 决定 | 当前状态 |
| --- | --- | --- |
| 空状态“新建 Git 仓库” | 移除插件入口；使用 VS Code 原生初始化仓库能力 | 已明确删除 |
| 空状态标题“当前工作区未发现 Git 仓库” | 保留 | 原型与正式共享面板已映射 |
| 空状态说明“可以在工作区根目录新建仓库，或递归搜索子目录中的现有仓库。” | 删除 | 已明确删除 |
| 空状态“搜索所有子目录” | 保留能力并移入 Git Tree，不再显示为空状态大按钮 | 原型与正式共享 Tree 已接，运行中可停止 |
| 仓库行 `+` | 保留登记已有仓库，不是初始化 | 提示改为“登记已有 Git 仓库”；原型登记仅内存模拟 |
| 仓库行刷新 | 保留，明确范围 | 提示改为“重新发现并刷新仓库”；复用原发现与刷新链路 |
| 仓库行 `−` | 仅从“我的仓库”移除；不得删除磁盘仓库 | 保留；修复 display 覆盖 hidden 导致空态误显，非外部登记仓库不显示 |
| 仓库选择 Combo | 保留完整路径 tooltip、空状态和不可用原因 | 原型与正式已核对；切换日志记录新仓库，不再误报上一个路径 |
| 递归发现子目录仓库 | 保留；这是插件的工作区级增强能力 | 原型模拟搜索/停止已实操；正式 Controller 原发现算法不变 |
| commit 简报 | 默认显示 2 条，其余按需展开 | 正式首次读取/刷新改为 2 条；原型 17 条样例显示 2 条与“更多 commit（已加载 15）” |

Git 简化只删除已明确重复的入口，不删除 Git 初始化的底层命令契约，直到确认没有其他消费者；底层能力若保留但 UI 不再暴露，必须在最终清单中注明。

## 验收与归档

- 原型先体现最终按钮位置、弹窗层级、状态和日志，再映射正式 UI。
- 每个迁移项必须有单元测试；弹层必须有真实浏览器的深色、浅色、高对比和窄 Primary 点检。
- KT Auto Code 使用并列 Wing 的 `pnpm ext:dev:prepare` 通过后，才可声明本地集成通过。
- 两个仓库分别阶段提交；不改版本、不发布、不推送，除非用户另行明确要求。
- 位于 `Phoenix/worktrees/<repo>` 的消费者先建立 `worktrees/phoenix-wing` 到明确活动 Wing 检出的
  仓外符号链接；规则已写入 `AGENTS.md` 与《本地 Wing 并列开发》，链接不进入 Git 或发布制品。

## 2026-09-08 阶段归档：Wing 组件与 AutoBuild 清理正式接入

本阶段已经完成：

- `PnwCombo` 迁入 Wing，KT Auto Code 仅保留薄适配层；旧本地实现与重复测试已删除。消费端阶段提交：`3927af4`。
- AutoBuild “执行”按钮组固定为 `脚本 / 预检配置 / 启动 / 停止 / 清理`；“清理”只负责打开 Wing 对话框。
- 对话框可直达规则、Git 强制恢复和 CMake 三种方式。ROOT_DIR 与工作目录是不同目标；项目 CMake
  删除整个 `build`，共享工作目录 `build` 只清空内容并保留目录。
- Host 对请求做方式、目标、长度和重复 ID 校验；预览冻结路径 / 文件树 / Git HEAD 与状态，执行只接受
  同一 document、draft revision、请求和 preview token，配置变化后旧预览失效。
- Git 强制恢复明确展示高风险确认，并由 Wing 执行 `reset --hard HEAD` 与 `clean -ffdx`；不会通过普通
  规则清理入口静默触发。
- 规则编辑值回写当前 AutoBuild 草稿的 `rootCleanupYaml`，后续随用户保存进入项目 JSON；默认不写入
  `Kt* / KTC* / PNX*`。
- 每个实际删除的顶层目标单独写入原生 Output；清理不启动 fetch、checkout、link、export 或 build。
- 原 `清理仓库`、`手动清理 Root` 两个常驻块，以及旧的 Windows-only PS1 clean-only Controller
  action 已删除；这是入口/实现合并，不是功能删除。PS1 与 sample 文件仍保留用于脚本导出、示例和回退。

本阶段自动证据：TypeScript 无错误；AutoBuild companion 27 项、Primary / ViewModel / 架构 10 项、
清理投影与 payload 5 项、Controller 14 项均通过；整库 1230 项通过、1 项原有跳过。Webview Preview
Server 的 6 项 loopback 测试在允许本机监听后通过。`pnpm ext:dev:prepare` 也已通过，metafile 证明
6 个 Wing 包均来自并列仓库、consumer `node_modules` 命中 0；为当前 worktree 建立了不入 Git 的
`worktrees/kt-auto-cad → kt-auto-cad` 活动检出链接。仍待完成：真实 Extension Host 的主题与危险确认
人工点检、Run 接入同一清理对话框，以及后续 Git / Auto Code / BUILD_MANIFEST 阶段。

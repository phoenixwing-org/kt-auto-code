# 固定与排序菜单控件

状态：current

Owner：KT Auto Code maintainers

适用版本：0.6.x 起；0.8.3 Toolbar Strip 整改

最后规则确认：2026-09-05

## 1. 定位

“固定与排序菜单”是挂在 Toolbar Strip 最右侧唯一 `…` 下的独立弹出控件，用于统一管理 Code、CAD 及后续模块的工具入口。

它不是第四个主区域，也不改变“Toolbar Strip → 目录 → 当前工具”三段的数量、顺序和职责。展开/紧凑两态共用同一个 `…`。

Toolbar Strip 的固定职责为：

- 左侧箭头直接切换“图标与文字 / 仅图标”；不显示独立“工具栏”标题行，不再提供密度按钮或完全隐藏态。
- 中间始终只有一份 Ribbon DOM：展开态保留现有图标文字并自然多行，紧凑态为单行仅图标轨道。
- 唯一 `…` 始终固定最右；两态都可打开菜单，弹层不得挂在可能滚动的 Ribbon 轨道内。
- 打开 `…` 后焦点必须直接进入菜单首个可操作项；不得从 Primary 原位置继续 Tab 到整页末尾才进入弹层。
- Code/CAD 不常驻 Header，而是在 `…` 的树形分组中使用 checkbox 控制显示。
- Ignore 与 Settings 位于 VS Code 原生 View Header，不属于 Toolbar Strip；插件级搜索和诊断也不进入 Toolbar Strip 固定操作位。

## 2. 用户点检

按以下顺序点击即可完成一轮人工验证：

- [ ] 点击 Toolbar Strip 最右 `…`，只出现一个固定与排序菜单。
- [x] 缩窄 Primary 侧栏，菜单仍完整可见，右侧 checkbox 不被裁切，内容过长时在菜单内部滚动。
- [x] Code、CAD 分组标题清楚，并可分别折叠和展开；checkbox 可控制模块显示。
- [x] 已固定工具始终显示钉子；未固定工具平时不显示钉子，鼠标移入或键盘聚焦该行时才显示。
- [x] 点击钉子可固定或取消固定，菜单不关闭，工具栏立即更新。
- [x] 点击工具名称可打开对应工具；未固定工具只临时显示，不自动变成固定。
- [x] 拖动整行可在同一 Code/CAD 分组内排序，并显示插入位置。
- [x] 使用上移、下移也能完成相同排序；首项不能上移，末项不能下移。
- [x] 固定或排序后菜单保持打开，不需要反复点击 `…`。
- [x] 尝试跨 Code/CAD 拖动时不改变工具归属和顺序。
- [x] 关闭并重新打开侧栏或重启 Extension Host，固定状态和顺序仍保留。
- [x] 只剩一个可见模块时不能继续取消该模块。
- [x] 打开菜单后焦点进入菜单；纯键盘 Tab 可到达未固定钉子并完成操作。
- [ ] 左侧箭头在展开/紧凑间切换；展开保留现有多行图标文字效果，紧凑为一行小图标，工具顺序不变。
- [ ] 两态都没有额外“工具栏”标题行和密度按钮，唯一 `…` 始终固定可达。
- [ ] 原生 Header 的 Ignore 打开独立 Ignore 工具；Toolbar 模式不受影响。

### 2.1 人工点检记录（2026-08-09）

> 这是 0.6.x 当时界面的历史回执。其中“工具栏折叠 / 显示密度”已由 2026-09-05 获批的“展开 / 紧凑”单箭头两态取代，不能作为当前实现依据。

用户已在固定测试 worktree 中确认本轮核心交互通过：

- 从菜单点击 Run 可正常打开工具；
- 工具顺序调整正常；
- 钉子固定与取消固定正常；
- Code/CAD 分组及折叠、展开正常；
- 工具栏折叠时 `…` 正常弹出，显示密度会自动展开并立即显示切换效果；
- 整行拖动及上移、下移后菜单保持打开；
- 搜索替换已收紧顶部间距、移除常驻提示并降低输入行高度；
- 原生 View 顶部不再重复显示 Code、CAD 和布局入口。
- 跨 Code/CAD 拖动被拒绝，重启后固定与顺序保留，最后一个可见模块不能取消；
- 搜索与替换均已实际执行，紧凑化只改变布局，不影响功能。
- 极窄侧栏中 checkbox 不裁切，菜单内部滚动正常；
- 打开菜单后 Tab 焦点直接进入菜单，并可在内部切换和操作钉子。

以上结论只对应 2026-08-09 的旧形态点检。清单中 2026-09-05 新增的 Toolbar Strip 两态、无独立标题/密度按钮和原生 Header Ignore 入口仍需按未勾选项重新人工确认。

## 3. 交互规则

### 3.1 分组

- 工具按贡献声明中的 `moduleId` 分组，当前先显示 Code 与 CAD。
- 分组使用“折叠箭头 + 模块 checkbox + 标题”的树形行，不用颜色单独表达展开状态。
- 折叠箭头只控制菜单内工具列表；模块 checkbox 只控制该模块是否显示在 Ribbon，两项操作不得绑定。
- 至少保留一个可见模块；最后一个可见模块的 checkbox 不允许关闭。
- 分组折叠只影响本次菜单会话，不写工程配置，也不改变工具固定状态。
- 新模块沿用同一模型，不为每个模块增加独立 `…`。

### 3.2 固定

- 已固定：钉子常显，`aria-pressed="true"`，提示“取消固定到工具栏”。
- 未固定：钉子默认隐藏；整行 hover 或 `focus-within` 时显示，提示“固定到工具栏”。
- 隐藏仅是视觉收敛，键盘到达该行后必须能看到并操作钉子。
- 固定操作只改变 `pinnedToolIds`，不得顺带打开、关闭或执行工具。

### 3.3 排序

- 整行是拖动来源，用户不必抓取一个很小的图标。
- 只允许同模块排序；跨模块拖放必须被拒绝。
- 放置位置使用目标行上半区/下半区判断 `before` 或 `after`，并显示插入提示。
- 上移、下移是拖动的键盘等价入口，不得只提供鼠标拖动。
- 排序更新稳定的 `toolOrder`；取消固定不丢失该工具在模块内的相对位置。

## 4. 控件边界

建议控件名为 `KtcRibbonCustomizationMenu`，注册为 `ktc-ribbon-customization-menu` Web Component。控件只接收可序列化 ViewModel，并发出语义事件：

```ts
interface KtcRibbonCustomizationMenuModel {
  tools: Array<{
    id: string;
    title: string;
    shortTitle?: string;
    moduleId?: string;
    moduleTitle?: string;
  }>;
  pinnedToolIds: string[];
  visibleModuleIds: string[];
}

type KtcRibbonCustomizationAction =
  | { kind: "open"; toolId: string }
  | { kind: "togglePin"; toolId: string }
  | { kind: "toggleModule"; moduleId: string }
  | {
      kind: "move";
      sourceId: string;
      targetId: string;
      placement: "before" | "after";
    };
```

控件内部负责：

- Code/CAD 树形分组和会话内折叠状态；
- 钉子、hover/focus、拖放目标和上移/下移按钮；
- 菜单行的键盘焦点、ARIA 和高对比度样式；
- 把用户操作转换为语义事件。

控件不得负责：

- 调用 `acquireVsCodeApi()` 或直接发送 Host 消息；
- 保存 `globalState`、`workspaceState` 或工程设置；
- 决定工具属于 Code 还是 CAD；
- 打开工具、执行命令或修改工作目录。

父级 Webview 负责把弹层挂到 Webview 顶层、锚定 Header `…` 并约束在可视区内，再把控件事件转发给 Extension Host。Host 校验工具 ID、模块边界和排序合法性，规范化后保存 `pinnedToolIds` 与 `toolOrder`，再广播新快照。

## 5. 复用规则

- 后续出现同类“固定 + 排序”场景时复用控件和事件契约，不复制 DOM/CSS 到业务页面。
- 调用方可替换标题和数据，但不得绕过同模块排序、键盘等价入口及持久化边界。
- 复用控件不等于新增主 Block；三 Block 外层契约仍以[前端开发规则](./前端开发规则.md#11-锁定的三-block-外层契约)为准。

## 6. 自动验证

- 控件可幂等注册，源码不包含 Host API、工作区或存储调用。
- ViewModel 可生成折叠分组、工具行和正确的固定状态。
- 模块显示、打开、固定、取消固定、拖动、上移、下移均只发一次对应语义事件。
- 未固定钉子具有 hover 与 `focus-within` 可见规则；已固定钉子常显。
- 同模块移动成功，跨模块移动由 Host 拒绝且布局不变。
- 菜单在窄侧栏内夹紧并内部滚动；深色、浅色和高对比度主题均有边框、焦点和拖动提示。

## 7. 非目标

- 不支持把工具拖到另一个模块。
- 不改变三个主 Block 的外层结构。
- 不增加另一套 Ribbon 布局存储。
- 不引入第三方拖拽库或新的 Webview。

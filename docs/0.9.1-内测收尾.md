# 0.9.1 内测收尾

状态：0.9.1 本地内测收口完成；两个自动代码 Block 已统一并点检，最终候选及回执已独立归档，不是公开发布

最后核验：2026-09-10

## 本轮冻结范围

- 开发工作树为 `worktrees/kt-auto-code-project-rename-analysis` 的 `v0.9.1`，用户明确以 0.9.1 收尾；主工作目录当时为 `test1`，不在主仓提交、合并或移动分支。
- 五项仅 Primary 工具：成员排序、ASCII、编码修正、UUID、CAA UI 的内部文字工具栏、共享组件、Preview 与正式 Host 接线。
- Git 正式修正：当前分支可达历史及分叉、连续主线选择、安全分支切换和失败对话框、长说明截断、remote 本地跟踪标记及最新提示交互。
- 自动代码收尾回归：JSON Right 内容高度链、预检详情可用高度及尺寸更新；Right 激活恢复对应 Primary，只投影当前 JSON，不运行工具、不关闭其他 JSON 或抢编辑焦点。
- 自动代码 Header 合并为“自动代码 · JSON 文件名 @ 目录”与右侧五项操作；预检结果由共享组件提供单个可折叠 Header，筛选、路径和计划状态不再重复。
- 最后反馈修正：参数表与预检结果共用 Block Header 样式，标题与空白处也可折叠，工具/筛选/路径开关独立；不改变 Body 滚动或生成规则。JSON 文档标签改名延至 0.9.2，仅文件名、同名不加目录，见[当前路线 TODO](current-roadmap.md)。
- 生成规则独立固化为 `@codegen-rules-version 1.0.0`，包含已完成的生成修正；旧预检缓存失效且隔离 runtime identity。规则升级清单同时写入 Auto/Wing `AGENTS.md` 与 [生成规则版本](codegen-plan/Codegen生成规则版本.md)。
- Git“合并区间”Right 原型按用户决定移至 [0.9.2](0.9.2-Git合并区间原型计划.md)，不是本版未完成阻塞项；清理快捷栏、Phoenix 缓存清理仍为后续 TODO。
- 保留 `.phoenix/cache`。顶层 `.cache/clangd/index/*.idx` 是语言服务索引，不修改排序算法，不迁移或删除此目录。

## 最后修订与浏览器点检

Wing `develop@cfb0cf52e67f7f5dacdce4c6d127afd2febec3bb` 已统一参数表与预检结果的 opt-in 折叠 Header：共享样式与原生 disclosure button，保留默认不可折叠组件、Body 滚动边界和各自状态。此次纯 UI 修改不升级 `@codegen-rules-version 1.0.0`。

2026-09-10 使用真实 Right Shell HTML 与正式共享 bundle 的本地浏览器内存夹具实测：两 Header 均约 34px 高、13px 字体、600 标题字重、3px/7px 内边距和 4px 圆角；点击标题、非工具按钮区域以及 Enter/空格均可折叠。预检筛选和路径勾选不触发折叠，折叠后保留选择；浏览器无 error/warn。额外 happy-dom 组件回归覆盖 Header 空白内边距、箭头及单次事件。此夹具不写用户工程，也不代替真实 VS Code 的跨 JSON 焦点、安装和全主题矩阵。

本次 Wing Codegen 33 文件、148 测试及类型检查通过；完整 `ext:dev:prepare` 中 Auto 248 文件、1772 通过、1 条件跳过，CAD 独立 Registry 14 文件、50 测试通过。干净源码重建与新包回执登记如下；旧候选保留不覆盖。

## 最终收口候选

- Auto `v0.9.1@e40702fe0cb96122e40deec6de1c7c7ab806de6f`，版本 `0.9.1`；构建和打包时源码工作树干净。后续回执文档单独提交，不冒充包内源码 OID。
- Wing `develop@cfb0cf52e67f7f5dacdce4c6d127afd2febec3bb`，根包 `0.7.3`；仅原有用户 `docs/plan.md` 未提交且未改动，SHA-256 `ebdfa005c12a8a7a2e32a8b90d86c32d345130fbe7021d2bfdb35fe0f6c67c1a`。不是 Wing 全树干净的发布证明。
- 源码冻结后重新通过完整 `pnpm ext:dev:prepare`，Auto 248 文件/1772 通过/1 条件跳过，CAD 独立 Registry 14 文件/50 通过；六项 Auto Wing 来源为并列仓库、consumer node_modules 命中 0。Auto 类型、114 份文档、260 源文件/27 pure graph/25 View root 的架构门禁通过；manifest/lockfile 精确 Registry 依赖未改。
- VS Code `1.136.2` 的真实 Extension Host 15 项代表流程再次通过，扩展版本 `0.9.1`；隔离 fixture 为 `/tmp/ktc-eh-kLlZ26`，不写用户工程。
- `ext:package:local-wing` 与 `ext:verify-artifact --local-wing` 通过：`dist/vsix/kt-auto-code-0.9.1.vsix`，66 文件、877316 字节，SHA-256 **`69f91c5121a82d8aa3519551b70727ebd3317aa383762718e648b479c9eb2d1e`**。
- 独立归档：`dist/vsix/0.9.1-internal-closeout-20260910/`，含同哈希 VSIX、`.sha256`、`.local-wing.json`、`extension-host-smoke-v1.json`、`browser-block-check.json` 与完整来源/边界 `freeze.json`。旧 0.9.0 和两份前序 0.9.1 独立归档保持不变。
- 已用受控 `pnpm ext:dev:code` 打开新 Host 快照 `kt-auto-code-local-host-BUBPtd`，源码来源同上；它与后续仓库构建隔离，旧窗口不会热更新。启动成功不代表下列安装/跨 JSON 焦点/全主题人工矩阵完成。
- 新包单独复核 SHA、文件数和敏感信息；没有发现新增凭据或个人邮箱，但内嵌本地 Wing 路径的内部包边界仍然存在，详见下节。`publishable: false`，不推送、不打 tag、不公开发布。

## 前一候选来源与制品（由最后 Block 修订取代）

- Auto 版本为 `0.9.1`；前一候选构建/打包源码冻结于干净的 `v0.9.1@3cecd4538c82b7c33156af29dfceca70871ba60b`（Header 与规则版本）。前序功能提交：`e9d57df`（五项 Primary）、`b3d2e9d`（Git）、`f16a99c`（开发归档）、`acfc625`（自动代码滚动与 Primary 联动）。回执文档在打包后单独提交，不冒充包内源码 OID。
- 本地 Wing：`develop@ec38785b6dda8fcadf2763aacf116ee80af2e686`（基于 `719b6b78fa` 增加独立规则版本及预检折叠 Header），根包版本仍为 `0.7.3`。已有用户修改 `docs/plan.md` 不动；随包来源记录明确这一工作树状态，不冒充 Wing 全树干净。
- Auto manifest/lockfile 的精确 Registry 依赖保持原样，不加入 `link:`、`file:`、override 或本地绝对路径。
- `ext:package:local-wing` 和 `ext:verify-artifact --local-wing` 已通过；制品为 `dist/vsix/kt-auto-code-0.9.1.vsix`，66 文件、876166 字节，SHA-256 为 `c9400a7917351c32cb5e216957afef9c14c6cd6190a300ac23564ac773e5ad23`。
- 独立归档：`dist/vsix/0.9.1-internal-final-20260910/`，包含同哈希 VSIX、`.sha256`、`.local-wing.json`、真实 Host 回执 `extension-host-smoke-v1.json` 和完整来源/边界 `freeze.json`。归档不包含用户工程或 Wing 的用户文档改动。
- `publishable: false`：本地 Wing 已嵌入包，不要求测试机器存在开发源码目录；此来源不能用作 Marketplace/Registry 正式发布证明。
- 0.9.0 旧内测包保持原样，SHA-256 为 `6666b974464f32051acca5fedb09a224a138c4b006ee7bca097be06641b4bddd`。
- 新增 Header/规则版本前的 0.9.1 中间候选已保留在 `dist/vsix/0.9.1-internal-before-codegen-header-20260910/`，SHA-256 `e168858d547933bee00e759031aadbb78cff14ab174f17ce146d7db5ea298c64`，来源 Auto `e237382` / Wing `719b6b7`；标明被后续候选取代，不作为最终交付。

## 前一候选自动验收

- 已完成前序基线：246 测试文件，1745 通过、2 条件跳过；Git 合并 View 的 36 条交互回归通过。
- [x] Node 22.23.1 / pnpm 10.15.1：Auto 默认 Registry 基线 248 文件、1770 通过、3 条件跳过（其中一项仅适用于新 Wing 折叠能力）；类型、114 份文档、260 源文件架构与六项精确 Registry 依赖门禁通过。Wing Codegen 32 文件、143 测试及类型检查通过。
- [x] 完整 `pnpm ext:dev:prepare`：Auto 本地 Wing 测试 248 文件、1772 通过、1 条件跳过；六个 Wing 包来自并列源码且 consumer node_modules 命中 0。CAD 独立 Registry 的 14 文件/50 测试、类型、架构与构建通过，不称作 CAD 本地 Wing 验收。
- [x] VS Code 1.136.2 中真实 Extension Host 的 15 项代表流程与回执通过，扩展版本为 0.9.1；运行于 `/tmp/ktc-eh-xxnkzR` 的隔离 fixture，不写用户工程。
- [x] 本地 VSIX、内容门禁（含实际 renderer 的规则注释）、来源记录、独立归档副本与哈希一致性通过。

前一候选曾使用受控 `pnpm ext:dev:code` 启动 Host `kt-auto-code-local-host-FPFPc5`，来源 Auto `3cecd45` / Wing `ec38785`；现已被上节最终快照取代，旧窗口不会热更新。启动成功不等于自动完成下面的外观和焦点点检。

自动代码新增测试覆盖 Right A/B 切换的草稿投影、关闭/隐藏 Primary 后恢复、焦点保留和迟到激活隔离，以及默认插槽高度与预检详情预算。已更新真实共享 Right Shell 的浏览器布局夹具。2026-09-10 用户重新启动后明确确认：整体上下滚动、下方内部滚动及原有滚动锁定行为均已恢复；这是用户实测回执。此前自动界面点检曾受锁屏和权限审批超时影响；本次两 Block Header 的浏览器点检已经完成并记录在上节，跨 JSON 激活/焦点仍单列真实宿主点检。

## 交付后的人工点检

以下是内测验收项，不宣称已经通过，也不阻止交付标明边界的内测包：

1. 在独立测试 Profile 从 0.9.1 VSIX 安装并重载；确认扩展版本及本次包 SHA-256。
2. ASCII/编码：可恢复文件的预检、取消、确认写入、结果定位与项目配置。
3. 排序/UUID：选中子集、取消/确认、差异；排序还原；UUID 两种策略变更后必须重扫，旧计划不得写入。
4. CAA：真实 Desk Tools 的机器设置、连接与外部打开；Windows/CAA 工具链不借 macOS 模拟验收。
5. Git：最新正式 Host 中 remote 仅点击展开、长文本停留 500ms、普通 branch 无专属 hover；临时仓库中验证切换确认/取消/失败和区间预检。真实合并/撤销只在可丢弃仓库中测试。
6. 浅色、深色、高对比、窄栏及重复关闭/打开；原有 0.9.0 高风险与跨平台边界继续见 [0.9.0 内测说明](0.9.0-内测说明.md)。
7. 自动代码 JSON Right：长参数表外层滚动、预检列表与固定详情、详情代码末行和横向滚动条；窄/矮窗口和工具栏换行后仍可达。切换不同 JSON Right、关闭/隐藏 Primary 后重新激活，左侧显示对应 JSON 且焦点留在 Right。极矮窗口保留既有详情 240px 最小高度，依赖外层滚动，不保证整块同时适配。

明确未实现：[构造函数结束标记缩进](bug/自动代码-构造函数结束标记缩进.md) 只记录用户样例、两组最小 input/expected，未改 renderer 的对齐规则，未运行 Windows clang-format，也未批量改写用户源码。

## 文档与敏感信息点检

本轮只读检查了 Auto 已跟踪 README、CHANGELOG、package、docs/scripts 共 173 文件，现有候选包内 66 项及 Wing 本轮文件；未检出真实 token、私钥、带密码 URL 或内网地址，这不是完整 Git 历史或全机器安全扫描。三处非分发文档的个人路径/邮箱示例已改用 `<PhoenixRoot>` 和 `developer@example.com`，保留问题与历史事实。

本地构建的 `dist/extension.js` **仍嵌入开发 Wing 的绝对路径**，这是来源标识而非凭据，不只存在于外部回执。此信息随本地 VSIX 分发；该包只用于已授权的本地内测，不应作为可公开外发的脱敏包。正式对外分发必须走不携带本地路径的 Registry 发布流程。

## 正式发布停止线

本轮只做本地内测收尾，不执行 push、tag、Marketplace 上传或 npm 发布。公开发布前须确认必需的 Wing scoped 能力已经进入 Registry，更新并验证精确依赖，在 Node 22/固定 pnpm 的干净 detached 发布工作树重新运行 `release:preflight`、Registry `release:check` 和 `release:receipt`；不得用本地 Wing 回执代替。用户的最终人工验收及发布授权另行确认。

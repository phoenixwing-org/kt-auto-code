# 0.4.0 单 Block 完成审计

> 审计日期：2026-07-15。目标完成与否按证据判定；自动测试不能替代真实 VS Code Extension Host 的视觉和宿主行为验收。

## 要求与证据

| 要求 | 权威证据 | 判定 |
| --- | --- | --- |
| 操作界面、状态和结果位于同一 Sidebar Block | `ktAutoCode.modulePanel` Webview 同时渲染七个模块的控件、状态与结果；成员排序使用专用缓存区，其余模块复用紧凑结果组件 | 代码与包已证明 |
| 不再创建第二个结果 View | 清单只贡献 Ribbon 与 `modulePanel` 两个 Webview；七个旧结果 TreeView、标题菜单和行菜单均已删除 | 已证明 |
| 扫描缓存 | 成员排序 session 保存 snapshot、纯状态行、revision、选择集和运行期诊断；重新扫描才递增 revision | 已证明 |
| 筛选 | 默认只渲染变更组；`显示无变更文件` 控制第二分组 | DOM/脚本测试已证明 |
| 勾选及关闭后选择缓存 | 组/行 checkbox 发送 `reorderSelection`，后端 session 保存 `reorderSelectedUris`；同 revision 保留取消选择 | 纯函数与 DOM 契约测试已证明 |
| 预览 | pending 行发送 `preview`，动作控制器阻止非 pending；后端使用 `vscode.diff` 和虚拟只读内容提供器 | 调度测试与源码已证明；真实编辑器行为待人工确认 |
| 单文件/批量应用 | 行按钮与顶部按钮均发送 URI 集；控制器只转发所选 pending；写盘前比较扫描原始字节 | 控制器测试与源码已证明 |
| 应用后不自动逐个打开 Diff | `applySnapshots` 只写盘、刷新 Git 和更新状态，不调用 `vscode.diff`/`git.openChange` | 已证明 |
| Git Diff | 仅 applied 行显示并允许 `gitDiff`；调用 VS Code `git.openChange` | 控制器测试已证明状态门槛；真实 Git 扩展行为待人工确认 |
| 还原 | applied 行可还原；还原前比较当前文件是否仍等于本次排序结果，阻止覆盖后续修改 | 控制器测试与源码已证明 |
| 取消候选 | 行内 `×` 将缓存行标为 cancelled，列表隐藏但不重新扫描或删除会话 | 状态/控制器测试已证明 |
| 关闭/重开 | 标题 `×` 只清除 `modulePanelVisible`；工具状态保存在 Provider，Webview 初始化时重放 | 源码已证明；VS Code 是否按预期保留/恢复高度待人工确认 |
| 文件名和目录列宽 | 文件名 `flex: 0 0 auto`，目录 `flex: 1 1 0` 并省略，右侧状态为单字符 | CSS 测试已证明规则；实际字体和侧栏宽度待人工确认 |
| TODO 与发布物 | 两份点检、主计划、README、CHANGELOG 和人工验收清单已更新；VSIX 已生成 | 已证明 |
| 工作集范围 | 六个文件型工具在 Block 顶部提供固定下拉框，按模块记忆；后端按 ID 解析并在配置失效时显式报错，不再由扫描动作弹 QuickPick | 源码、DOM 契约与 exact-file 测试已证明；视觉行为待人工确认 |
| Ribbon 多打开状态 | 当前/后台打开/未打开使用三态样式；MRU 纯函数保证关闭当前项后恢复最近使用项，最后一个关闭才收起 | 纯函数与 DOM 契约测试已证明；颜色视觉待人工确认 |
| 自动测试 | TypeScript 通过；32 个测试文件、209 项测试通过；扩展构建与 VSIX 打包通过 | 已证明 |

## 剩余人工门槛

成员排序已由用户确认效果符合预期。安装 `/private/tmp/kt-auto-code-0.4.0-block-scope.vsix`，在真实 Extension Host 中继续点检其余模块：

1. 头文件与编码：预检/修复后结果原地更新，详情切换和文件打开正常。
2. 搜索替换：完整表单仍可用，预览/写盘结果与文件名高亮正常。
3. Ignore：分析、默认选择、手动勾选和追加所选正常。
4. UUID：策略选择、全选/单选、逐文件写盘、取消与 Git Diff 正常。
5. CAA：扫描、VS Code 打开、外部编辑器和工作集正常。
6. 所有模块均不产生第二个结果 TreeView，切换和重开保持缓存。

全部人工确认前不发布 0.4.0；若发现问题，只修正当前模块 Block，不恢复旧结果 TreeView。

# 2026-07-20 功能修复人工点检表

状态：current

Owner：KT Auto Code maintainers

适用版本：KT Auto Code 0.5.x / 0.6.0、KT Auto CAD 0.1.x、Desk Tools 0.2.x

最后核验：2026-07-21

## 1. 准备与记录

- [ ] 使用临时 Git 工作区或干净分支，记录初始 `git status --short`。
- [ ] 安装本轮生成的 `kt-auto-code-0.6.0.vsix`；回归 0.5.3 时使用已归档的对应 VSIX，测试 CAD 时同时安装 `kt-auto-cad-0.1.0.vsix`。
- [ ] 准备包含 `.h/.cpp/.md`、UTF-8 中文、GBK 中文、`.CATDlg`、`.FCStd` 和 `.gitignore` 的小型工作区。
- [ ] 记录 VS Code、Windows/macOS、三个产品版本与实际 Desk Tools 端口。

## 2. 搜索替换：全大写派生

1. 打开“搜索替换” Block，输入 `AutoCode → TomBuild`，勾选“文本”和“同时匹配全大写”。
2. 在同一文件中准备 `AutoCode` 和 `AUTOCODE`，点击“预览”。
3. [ ] 确认两处分别预览为 `TomBuild` 和 `TOMBUILD`；任意混合大小写不被猜测匹配。
4. [ ] 关闭再打开 Block，勾选状态保留；保存规则档案、切换其他档案后重新加载，状态仍保留。
5. [ ] 取消勾选后重新预览，只匹配 `AutoCode`，不匹配 `AUTOCODE`。
6. [ ] 点击“替换”但在 Modal 中取消，旧预览仍在，Block 不保持转圈，磁盘内容不变。

## 3. 关联规则候选摘要

1. 输入 `AutoCode → TomBuild`，源/目标前缀填 `KTC → KTM`。
2. [ ] 点击“常用规则”，标题区显示候选数和默认选中数，数量与列表 checkbox 一致。
3. [ ] 点击“CAA 规则”，确认摘要为 4 条候选、默认选中 2 条，默认项为 I/E 完整名称。
4. [ ] 已有搜索词被去重后，摘要数随剩余候选变化；无候选时提示可填写自定义规则。

## 4. 编码修正：UTF-8 / GBK 切换

1. 打开“编码修正” Block，先以 UTF-8 默认目标执行一次预检。
2. [ ] 将“默认目标”改为 GBK：下拉框不回弹，旧结果立即清空，状态显示“项目编码目标已更新，请重新预检”。
3. [ ] 关闭再打开 Block，仍显示 GBK；重载 VS Code 窗口后仍保留。
4. 若“项目覆盖”把源文件设为 UTF-8，先在“更多设置”改为继承；默认目标不应暗中覆盖显式分类策略。
5. [ ] 预检可无损转为 GBK 的 UTF-8 中文 `.cpp`，确认显示 UTF-8 → GBK；确认转换后检测为 GBK。
6. [ ] 再准备一次待转换结果，点击“按目标转换”后取消 Modal：状态回到 idle、旧结果保留、无文件写盘。

## 5. 头文件 ASCII 取消路径

1. 对包含弯引号或非 ASCII 内容的 `.h/.cpp` 执行预检。
2. [ ] 点击“修复”并取消 Modal：Block 结束转圈，显示“已取消修复”，旧问题列表保留，磁盘字节不变。
3. [ ] 未打开工作区时从命令面板触发预检，显示明确错误且不进入持续 running。

## 6. Ignore 分析、默认单选与缓冲区

1. 在包含 C++/Web 构建目录的工作区打开“忽略设置”，点击“分析当前工作区”。
2. [ ] 有多个安全建议组时，默认只勾选第一个可追加组；`review-required` 不默认勾选。
3. [ ] 手动多选后，主按钮显示“追加所选推荐（N）”，每组仍显示建议规则、已有数和阻止数摘要。
4. [ ] 追加所选组后，`.phoenix/.ignore` 文档为 dirty，未手工保存前磁盘字节不变；其他扫描立即读取打开缓冲区规则。
5. [ ] 手工保存后重新扫描，确认缓存失效并使用新磁盘规则。

自动边界证据：非法 preset ID/action、缺失或空推荐组 ID 会在 Host 入站守卫处拒绝；不需要在生产 Webview 中伪造恶意消息。

## 7. 工作集 dirty 防覆盖

1. 生成一组搜索替换、UUID、CAA 或成员排序结果，确保“加入工作集”可用。
2. 打开 `.phoenix/worksets.json`，做一个合法但不保存的修改。
3. [ ] 点击“加入工作集”，立即提示“有未保存修改”；不读取旧磁盘配置、不弹出工作集选择、不写盘。
4. [ ] 确认文档仍为 dirty，缓冲区修改原样保留。
5. [ ] 保存后重试，可正常选择工作集，且同一精确文件规则不重复追加。

## 8. CAA UI / Desk Tools 动态发现

1. [ ] 完全退出 Desk Tools，打开 CAA UI 并点击检测：显示离线，VS Code 不冻结。
2. [ ] 启动 Desk Tools，确认 CAA UI 显示 `127.0.0.1:<实际端口>`，且 `service.v1.json` 端口一致。
3. [ ] 事先占用 `48375` 后重启 Desk Tools，确认它在 `48376..48406` 中选择后续端口；插件不扫描猜测端口，仍通过注册文件连接。
4. [ ] 从 CAA 结果打开 `.CATDlg`，确认 Desk Tools 切换到正确工作区和文件。
5. [ ] 退出 Desk Tools 后重新检测，stale 注册不被当作在线服务。

## 9. 旧设置迁移与 Auto CAD 深度读取

1. [ ] 在旧 key 中填写 CAA command/args/endpoint 或 CAD provider manifest，启动新扩展；确认只迁入用户明确配置的值。
2. [ ] 预先填写不同的 `ktAutoCode.deskTools.*` 新值，确认旧值不覆盖新值；显式空新值也不复活旧值。
3. [ ] 只迁移旧 endpoint 时，`Discovery Mode` 为 `custom`；手动改回 `auto` 后重新使用动态注册。
4. [ ] Desk Tools 至少启动一次以生成 `installation.v1.json`，随后完全退出 Desk Tools。
5. [ ] 在 Auto CAD“读取”中执行 `.FCStd` 深度分析：读取器可用，不要求 Desk Tools 窗口或 HTTP 服务运行。
6. [ ] 填写错误 manifest 或篡改读取器，确认平台、Schema、SHA-256 或 protocol v1 验证失败，不降级绕过。
7. [ ] Auto CAD Ribbon 不出现独立“连接 Desk Tools”工具；服务连接属于 CAA UI，安装读取器状态属于 Auto CAD 读取功能。

## 10. 高对比度边框与 hover

- [x] 2026-07-21 用户回执：高对比度模式下 Primary、功能 Block 与“预检”等按钮的边框及 hover 效果测试通过。
- [x] 自动回归覆盖 Sidebar、Codegen Primary/预检、成员排序和关联规则组件的对比边框变量与 hover 契约。

## 11. Git Primary Block（0.6.0）

1. [ ] 在有 upstream 的测试仓库打开 Git Block，确认只出现 Primary Block，不新增 TreeView、编辑器页或独立配置模块。
2. [ ] 最近 commit 标题明确提示“勾选生成简报”；分别勾选 1 个和多个 commit，确认按列表顺序生成对应数量的两行简报，且不显示原始 Commit/Parents/Author/Committer/email/date/body。
3. [ ] 未勾选“Git 地址”时确认每条为 `仓库 origin/分支 **Commit:** 7位SHA ++ · 时间` 加下一行 subject；勾选后整份顶部只出现一次真实 remote URL，下面从 `origin/分支` 开始且不重复项目目录名。`++` 表示代码已更新。
4. [ ] 分别切换短选项“时间”“@审查人”；commit 内无审查人时从不可搜索的原生下拉切换默认/最近人员，确认全部候选可见；选择“＋ 输入新人员…”后可在原位置输入并保存，写入机器级 `ktAutoCode.git.reviewers`，且可在插件设置中删除或清空；原 commit message 不变。
5. [ ] 修改摘要后复制到群聊，确认换行、加粗标记和 `++` 保持；刷新不会写 Git。
6. [ ] 在临时仓库选择直线历史中间的连续提交，确认预检明确列出“合并区间”和“随后重放到 HEAD 的提交”；默认人员与时间来自所选最新提交，Author/Committer 时间按本机显示为 `YYYY-MM-DD HH:mm:ss`，不显示时区或 Unix 秒数，保存后 instant 正确。
7. [ ] 确认执行后所选区间变成一个节点、后续提交逐个重放、HEAD 最终文件树与执行前完全相同，并可用“一次撤销”恢复旧历史。
8. [ ] 验证脏工作区、非连续选择、merge commit、签名 commit 仍硬阻断；remote 已引用或其他分支/tag 占用时弹出确认，取消不改动，确认后只更新当前本地分支，相关引用仍指向旧历史，且不自动 push/force push/删除引用。
9. [ ] 预先创建同名 `refs/kt-auto-code/backup/<branch>`，确认执行后备份自动使用 `-2`（继续冲突则 `-3`），不覆盖已有引用。
10. [ ] 分别使用单根仓库、多根 `.code-workspace`、含 submodule/独立嵌套仓库的工作区打开 Git；确认原“工作区”行变为仓库下拉，至少选中活动文件所属仓库，下方只显示所选仓库。切换仓库后简报/合并不跨库；存在草稿时要求确认，执行期间不能切换；重载后恢复当前工作区上次选择且不写 `.vscode/settings.json`。

用户实测简报基线（2026-07-21）：未勾选 Git 地址时为 `PNXCaaStudy origin/sort **Commit:** 8f8f40e ++` / `新排序`；勾选后为顶部 remote URL 加多个 `origin/sort **Commit:** ...` 两行条目。时间位于每条第一行末尾。

## 12. Run Primary Block（0.6.0）

1. [ ] Run 位于 Git 后面；打开只做只读发现，不执行脚本、不创建 `.phoenix/run/`，默认只显示当前系统可运行目标。
2. [ ] 打开包含多个子目录的工作区，确认 Task、`ps1/bat/cmd/sh`、可执行文件与 CMake 目标按真实项目根分组；名称与相对路径使用一个连续可截断标签。
3. [ ] 参考 `PNXCaaStudy` 时，`KtCore` 显示为 CMake C++，各 `*Wsp` 显示为独立 CAA 项目，Framework/Interfaces/Tlb 不被误建为项目。
4. [ ] 每个 CAA 项目只显示一个逻辑 `MK` 和一个 `Run`；用户 Settings 的 `ktAutoCode.run.caaVersion` 作为本机默认，Primary 切换当前版本后 `.vscode/settings.json` 不变化，实际 Task 使用所选版本，原生 Task 无法安全覆盖时明确拒绝。
5. [ ] 为 MK 多选已发现的关联工程，再添加目录；确认 `.vscode/settings.json` 的 `ktAutoCode.run.caaRelatedProjects` 优先保存相对路径，同时去重、工作区外二次确认、危险路径拒绝，且来源明确切换为内置 runner。
6. [ ] 在未信任工作区中目标全部只读；信任后执行通用目标进入 VS Code Task Terminal，可停止，结束态与退出码进入最近运行。
7. [ ] 原生 Task 的 problem matcher 保留；CAA/CMake 生成任务的命名 matcher 能把 fixture 错误送入 Problems。
8. [ ] Windows 实机验证 CAA MK/Run 预检、厂商 batch 链、非零退出码和 CNext 停止；macOS/Linux 只核验置灰与发现结果，不宣称能运行 CAA。
9. [ ] 将包含当前 CAA project 与相对关联工程的共同父目录整体复制/移动后重新打开；确认设置文本仍为相对路径，运行预检基于新 project root 解析到新的绝对目录，不引用旧机器路径。

## 13. 完成记录

- [x] 2026-07-21 用户回执：0.6.0 Git 与 Run Primary Block 本轮人工测试均通过；平台专项步骤仍按上方清单分别保留证据。
- [x] 0.5.3 基线已使用 Node 22.14.0 运行全量门禁；114 个测试文件、563 项测试及双 VSIX 制品门禁通过。
- [x] 0.6.0 本地候选使用 Node 22.14.0 完成 Git Core/Node 定向测试、`pnpm ext:dev:prepare`、Auto 122 文件/583 项测试、typecheck、架构、文档、真实 Extension Host 与本地 Code VSIX 制品门禁；35 文件、525305 bytes、SHA-256 `92775e2b7461835e8365b762da3644882feaf237932a96fb2583370ba9cae120`。
- [x] Phoenix Wing 0.5.0 的 12 个 npm 包已发布并通过隔离 Registry 消费验证；Auto Code 的 15 处 Wing manifest 引用与 lockfile 已精确闭环到 0.5.0，不再使用不可发布的本地 Wing VSIX 作为正式候选。
- [x] 2026-07-22 使用 Node 22.14.0 完成 Registry 正式构建、122 文件/583 项测试、双 typecheck、并列 Wing 来源门禁和真实 Extension Host smoke；最终 `kt-auto-code-0.6.0.vsix` 为 35 文件、525810 bytes、SHA-256 `378ac17b78b0aefb62d0d4aeb970842b61ee3882f86ee97df2c7490a442b523c`，不包含本地 Wing 回执。本轮不执行 Marketplace 发布。
- [ ] 记录每个失败项的系统、工作区、操作步骤、Output 日志和截图。
- [ ] 确认最终 Git diff 只包含本轮预期的实现、测试与文档修改；取消路径和 dirty 防护项不应产生额外写盘。

更专项的 Desk Tools 步骤见 [CAA UI / Desk Tools 人工验收清单](CAA-UI-DeskTools人工验收清单.md)；旧 Block 全量回归见 [0.4.0 Extension Host 人工验收清单](0.4.0-ExtensionHost人工验收清单.md)。
